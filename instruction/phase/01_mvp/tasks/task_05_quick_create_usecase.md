# task_05_quick_create_usecase.md

## 配置層

application

## 目的

クイック作成ロジックを実装する。

## 実装内容

* 指定方向移動
* 指定マス数移動
* 候補交点の算出
* 空きチェック
* 下方向探索
* 探索上限処理
* 失敗判定
* 作成成功時の接続追加

## 要件

* グリッドロジックは domain を利用する
* UI に依存しない
* 理由コードまたは結果種別を返せるようにする

## 完了条件

* 入力パラメータから配置結果が決まる
* 成功 / 失敗理由を presentation 層へ渡せる