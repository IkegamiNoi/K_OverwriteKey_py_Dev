# レビュー目的

* オニオンアーキテクチャの土台が崩れていないか確認する
* コアデータ構造とルールが安定しているか確認する
* 保存形式と内部モデルの整合性を確認する
* UI 実装前に手戻りリスクの高い設計ミスを潰す

# アーキテクチャ観点

* [ ] ディレクトリ構成が仕様に沿っている
* [ ] domain 層が PySide6 に依存していない
* [ ] application 層が UI イベントに依存していない
* [ ] infrastructure 層が UI ロジックを持っていない
* [ ] 依存方向が外側から内側のみになっている
* [ ] key_input_policy.md に依存する内容が domain/application に漏れていない

# データモデル観点

* [ ] Node, Edge, Row, Column, Position の責務が明確
* [ ] ノードID・行ID・列IDの扱いが一貫している
* [ ] 1交点1ノード制約を表現できるモデルになっている
* [ ] content / structure / layout の3分割と内部モデルの対応が明確
* [ ] 将来の拡張に対して無理のない構造になっている
* [ ] 表示専用の状態が domain に混入していない

# ドメインロジック観点

* [ ] ノード追加ルールが明確
* [ ] ノード削除時の接続整理が定義されている
* [ ] 接続構造の整合性が保たれる
* [ ] 占有交点判定が一貫している
* [ ] 行・列・位置の整合性チェックがある
* [ ] 副作用なしでテストできる構造になっている

# ユースケース観点

* [ ] application 層が domain を適切に利用している
* [ ] クイック作成ルールが仕様通り
* [ ] カスタム作成ルールが仕様通り
* [ ] 下方向探索の扱いが明確
* [ ] 失敗時に返す結果が presentation で扱える形になっている
* [ ] Manhattan距離と同順位処理が実装可能な形で表現されている

# 永続化観点

* [ ] JSON 保存形式が仕様と一致している
* [ ] 保存・読込でデータが往復できる
* [ ] バリデーションの責務が整理されている
* [ ] ビュー状態が保存対象に混入していない
* [ ] 破損データや不正データへの最低限の対策がある

# テスト観点

* [ ] domain ロジックの単体テストがある
* [ ] クイック作成の成功/失敗テストがある
* [ ] カスタム作成の最近傍探索テストがある
* [ ] JSON 保存・読込テストがある

# ブロック完了判断

* [ ] UI 実装に進んでもコア構造を大きく作り直す必要がない
* [ ] 仕様書と実装の差分が把握できている
* [ ] 次ブロックへ進めるだけの安定性がある