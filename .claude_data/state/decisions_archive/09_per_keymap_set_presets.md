# decisions_archive: 09_per_keymap_set_presets

> phase 09（keymap_set ごとの個別プリセット）の判断履歴。**2026-08-16 完了**。
> 正本は `spec_detail/data_schema.md` **§5.10**（データモデル・移行規則 / 解決順序 /
> 編集と保存の契約〔書き込み前の判定 3 段・dirty・別名保存の複製・UI 契約〕/ 既知の制約）
> + **§5.8.8**（入口台帳。E5 の強制 OFF / **P1 = 表示用の再読込**）+ §5.5 / §5.4 / §5.1、
> および `codebase_map.md`。
> 暫定仕様 08（`instructions/history/08_per_keymap_set_presets.md`・**v0.10**）は**凍結済**（経緯の参照用）。
> 起票元 idea_08 はクローズ（`instructions/backlog/INDEX_done.md`）。

---

規範: [phase.md](../../../instructions/phase/09_per_keymap_set_presets/phase.md) /
主入力 = 暫定仕様 08（`instructions/history/08_per_keymap_set_presets.md`・**最終 v0.10**）。
**暫定仕様先行モード**（以下の記録は起票当時の版を指すことがある）。

## 版の変遷（要約・詳細は各節）

| 版 | 変えたこと | きっかけ |
|---|---|---|
| v0.3 | 初回確定（配置・旧キー再利用・フラグ・書き手 1 本・フォールバック・Import 強制 OFF）| ユーザー確定 |
| v0.4 | 移行手順に config.json の明示値を追加 /【I】即時解決を**撤回** /【L】別名保存で**複製する**へ反転 | Codex 敵対的レビュー |
| v0.5 | 【O3】config 外パスでの保存を**拒否** | task_05 の reviewer 指摘 |
| v0.6 | **v0.5 を反転** — 拒否せず**既定パスへ寄せて新規作成** + **パス変化時だけ dirty** | ユーザー確定 |
| v0.7 | 【O4】**保存先ガード**（`global/` 配下 / グローバルと同一ファイルなら拒否）| 2 本立てレビューが**独立に同じ穴**を検出 |
| v0.8 | 【§3-5】**残置パスも落とす** /【I】**再採用**（トグルで読み直し・【H2】撤回）/【O2】**config 外は読み出し可** | **実機目視 3 件** |
| v0.9 | 【S】**個別へ書く直前の上書き確認**（内容比較）/【I2】**OFF で開いた時点の一覧確定** | 完了判定前レビュー |
| v0.10 | 【S】を **3 択**（上書きする / 既存を読み込む / キャンセル）へ | **実機目視 項目 17** |

**反転が 2 回起きている**（v0.5→v0.6 の【O3】/ v0.4→v0.8 の【I】）。いずれも
**「拒否・保持は安全に見えて行き止まり / 非対称を生む」**という同じ形の誤りで、
**復旧導線があるか**・**読み書きの対称性**を先に問うべきだった、が教訓。

### 【起票時】idea_08 の昇格と設計確定（ユーザー確定 2026-08-09）

- `deep-reviewer`（起票時）= **修正要** → 確認事項を A〜F から **A〜R** へ拡充（v0.2）。
- ユーザー確定（v0.3）→ **`codex-adversarial-reviewer` = needs-attention（High 4 / Medium 2）** → v0.4。
- **確定の要点**: グローバル既定を `user/hotkey_presets/global/default.json` へ移す（**移行は手動・2 段**）/
  個別は `user/hotkey_presets/<stem>.json` / **旧キー `hotkey_presets_path` を個別パスとして再利用**し
  **新フラグ `hotkey_presets_individual` が真のときだけ読む**（**フラグ無しは常に OFF**）/
  **書き手はマネージャ 1 本** / 個別が読めなければ**グローバルへフォールバック** /
  **Import は強制 OFF** / **トグルは保存先の切替だけ**（一覧は差し替えない）/
  **別名保存では個別ファイルを複製**。
- **A（旧キー再利用）の判断根拠**: 意味は phase 08 以前へ戻る方向であり、**フラグを値で判定**すれば
  残置値は必ず無視される。加えて **G でグローバルを `global/` へ移したため、残置値はもう
  グローバルを指さない**（上書き事故が構造的に起きない）。旧版アプリが読んでも
  **未知キー無視 + 読込時無視でグローバルへ安全に倒れる**。
- **敵対的レビューで潰した穴**: ①config.json に**明示保存**があると手動移行が効かず
  **旧パス再生成のループ**になる（→ 移行手順を 2 段にした）②トグル即時解決が編集中の `_temp` と
  両立しない（→ **【I】を撤回**）③config 外パスがマネージャの保存先になり得る（→ 無効化を規定）
  ④別名保存の無警告共有（→ **【L】を反転して複製**）。
- phase.md の整合チェック（`reviewer`・限定）= **採用（修正不要）**。

### 【task_01】完了（2026-08-09）= グローバル既定パスの移動

- `HOTKEY_PRESETS_RELATIVE_PATH` を `user/hotkey_presets/global/default.json` へ変更し、
  `ensure_split_config_dirs` へ `global/` を追加（**`user/hotkey_presets` 直下の作成は残す**）。
  **production の差分は 5 行**。
- **互換フォールバック読みは実装しない**（仕様どおり）。「**旧既定にファイルがあっても読まれない**」を
  特性テストで固定した。
- 既定パスをハードコードしていた 5 箇所を**定数参照へ**更新 → 次の変更へ自動追随する。
- 実測: compile clean / `tests` **204**（+1）/ `tests_ui` **186** / smoke pass。
  件数 +1 の内訳 = 既存 1 件の**改名** + 新規 1 件。
- `reviewer` = **採用（完了可・指摘なし）**。

### 【task_02】完了（2026-08-09）= keymap_set のスキーマ追加

- `DEFAULT_CONFIG` へ 2 キー追加 + `ensure_config_compatibility` で正規化 +
  `build_runtime_data_from_split` のキーコピー + `build_keymap_set_payload` の返却へ挿入
  （**`trigger_set_path` の直後**。他のキー順は不動）。
- **移行規則は値のみで判定**。**`resolve_hook_keys_individual`（フラグ無し + 非空なら ON）を流用しない**
  = 流用すると phase 08 の**残置 `hotkey_presets_path` が個別指定として復活**する（§3-5）。
  真偽値でない値（`"true"` / `1` / `None`）は **False**。
- **OFF でも `hotkey_presets_path` を空文字化しない**（hook キーの「OFF なら常に `""`」とは別扱い・【N】）。
- **想定外の先行実装 → 修正して採用**: phase 08 の特性テスト 2 件
  （keymap_set payload に `hotkey_presets_path` を出力しないことを固定していたもの）は
  **phase 09 が意図的に覆す前提**のため、**削除せず新仕様の期待値へ更新**した
  （常時出力・OFF でもパス保持）。`reviewer` は「緩和ではなく検証内容の強化」と判定。
  → **phase 08 の受入条件 2 は phase 09 で上書きされる**。正本 §5.5 / §5.1 の改訂は **task_08** で行う。
- 実測: compile clean / `tests` **209**（+5）/ `tests_ui` **186**（増減なし）/ smoke pass。
- `reviewer` = **採用（完了可・指摘なし）**。

### 【task_03】完了（2026-08-09）= 解決順序の実装（**読込先が実際に分岐**）

- `load_hotkey_presets_file`（任意パス・`list | None`）を切り出し、`load_global_hotkey_presets` を
  **薄いラッパ**へ。**公開規約（`list | None`・正規化済み・例外を投げない）は不変**。
- 個別パスの**有効判定を 1 関数へ集約**（フラグが `is True` / 非空文字列 / **解決後が config 配下** /
  `config_root` 空なら無効）。判定は**比較専用 API**（`is_path_within`）で、**保存値は書き換えない**。
- 供給順 = **個別 → `None` ならグローバル → それも `None` なら置き換えない**。
  **`is None` で判定**（`[]` は falsy のため真偽判定で書くと「読めた空」がフォールバックしてしまう。
  受入条件 9 の規則を個別側でも維持するための要点）。
- `apply_global_defaults`・保存側・presentation は無変更。runtime へ内部キーを増やしていない。
- 実測: compile clean / `tests` **216**（+7）/ `tests_ui` **186** / smoke pass。
- `reviewer` = **採用（完了可・指摘なし）**。

### 【task_04】完了（2026-08-10）= 保存先の算出と書込先の切替

- `default_individual_hotkey_presets_path` を trigger_set と同じ流儀で追加
  （`slugify_file_stem` / フォールバック `default` / **衝突回避しない** / `split_base_dir` を取らない）。
  **グローバル既定が `global/` 配下にあるため stem が `default` でも衝突しない**（task_01 の狙いが効いた）。
- `save_hotkey_presets(..., stored_path)` を新設し `save_global_hotkey_presets` を薄いラッパへ
  （**読み出し側の 2 段構えと対称**）。**例外は送出のまま**（成否変換は presentation）。
- 書込先の決定は `resolve_hotkey_presets_save_path` に集約し、**task_03 の判定を再利用**。
  **ON + config 外は空文字（グローバル）へ倒す**＝個別へ書かない。
- presentation は `write_presets(presets, *, stored_path)` へ改名（旧名の残存ゼロ）。
  `App.save_hotkey_presets` は**成功時のみ** `data["hotkey_presets_path"]` を確定値へ反映し、
  **フラグは変えない**（切替は task_05）。**失敗時は `data` を一切変更しない**。
- **【H】ON にした時点ではファイルを作らない**（実体は保存時に初めて作られる）。
- **参考指摘（非ブロッキング・候補送り）**: `default_individual_hotkey_presets_path` は
  `HOTKEY_PRESETS_RELATIVE_PATH` へ `os.path.dirname` を 2 回かけて `user/hotkey_presets` を導いており、
  **定数の階層が変わると例外を出さず誤ったディレクトリを返す**。リポジトリ内の既存慣用手法の範囲内で、
  値ベースのテストが担保しているため今回は据え置き（専用定数を切る代替案あり）。
- 実測: compile clean / `tests` **220**（+4）/ `tests_ui` **189**（+3）/ smoke pass。
- `reviewer` = **採用（完了可）**。

### 【task_05】完了（2026-08-10）= 切替 UI と OK / キャンセルの契約

- **仕様の解釈を 2 点確定**（暫定仕様 08 が文言レベルまで規定していなかった箇所。タスク定義へ明記した）:
  - **保存先の表示は 2 行に分ける**。「保存先」= **チェックに追従**（§3-4）/
    「一覧の出どころ」= **ダイアログを開いた時点で固定**。
    **トグルで一覧を差し替えない**【I 撤回】以上、1 行に混ぜると
    「トグルへの追従」と「フォールバック中の告知」が両立しないため。
  - **表示状態の判定は application 側**（presentation で解決順序を組み立てない。task_04 と同方針）。
- `resolve_hotkey_presets_save_path` へ **`individual: bool | None` の override** を追加
  （**runtime は書き換えず局所コピーで判定**。`None` は現行どおり）。
  `describe_hotkey_presets_source` を新設し、**`individual_state`（off / active / missing / invalid）×
  `displayed_source`（individual / global / builtin）の 2 値を独立に返す**
  （**優先順位を付けて片方を隠さない**。既存 3 関数を再利用し判定を二重化しない）。
- `App.save_hotkey_presets(presets, *, individual=None)` の 4 分岐を確定。
  **ON→OFF は保存 API を呼ばず**フラグだけ False にし、**`hotkey_presets_path` は保持**【N】、
  **グローバルを読み直す**【H2】。**dirty はフラグの値が実際に変わったときだけ**。
  **失敗時は `data` を一切変更しない**（先にフラグを立てて戻す形にしない）。
- 文言は **Tk 非依存の純関数 `format_preset_manager_source_labels`** へ分離
  （`PresetManagerDialog` は `grab_set` / `wait_window` を持つためテストから構築しにくい）。
- **【Q】の再評価はダイアログを毎回構築し直す性質で満たす**（通知の仕組みは足さない＝過剰実装の回避）。
- **保留（ユーザー判断待ち・task_06 着手前に確認）**: `reviewer` の参考指摘 =
  **個別 ON かつパスが config 外（無効）のとき、UI は「無効」と表示するのに OK の保存先は
  グローバルへ倒れる**。`resolve_hotkey_presets_save_path` は「パスが非空なら既定へ差し替えない」ため
  既定の個別パスへも落ちない。**暫定仕様 08 §2【O2】は読み出しのみ規定**しており、
  書き込み側は未定義。**専用のつもりの操作でグローバルが上書きされ得る**点が
  「グローバルの保護」観点と衝突する疑い。選択肢 = (A) 現状維持 / (B) 既定の個別パスへ逃がす /
  (C) 保存を拒否して理由表示。**task_04 由来のため task_05 では不変で通した**。
- 実測: compile clean / `tests` **222**（+2）/ `tests_ui` **196**（+7）/ smoke pass。
- `reviewer` = **採用（完了可）**。

### 【仕様確定 v0.5】無効な個別パスでの保存を拒否する（2026-08-10・ユーザー確定）

- 発端 = **task_05 の `reviewer` 参考指摘**。task_04 由来の未定義挙動で、
  **個別 ON かつ `hotkey_presets_path` が config 外のとき、UI は「無効」と表示するのに
  OK の保存先はグローバルへ倒れて上書きされ得た**。
  `resolve_hotkey_presets_save_path` は「パスが非空なら既定へ差し替えない」ため既定の個別パスにも落ちない。
- **ユーザー判断 = (C) 保存を拒否して理由表示**。
  - 却下 **(A) 現状維持**（UI が「無効」と告知している状態で黙って別の場所へ書くのは事故。
    **グローバルライブラリを破壊し得る**）
  - 却下 **(B) 既定の個別パスへ逃がす**（記録済みパスから保存先が黙って変わり、同種の驚きを生む）
- **暫定仕様 08 を v0.4 → v0.5 へ改訂**し **【O3】を新設**
  （§2 / §3-3 / §3-4 / 受入条件 15 / §4 の却下記録）。**フェーズ中の正は暫定仕様**のため
  正本 `spec_detail/` の更新は **task_08** で行う。
- **【O2】読み出しは従来どおりグローバルへ倒す**。**この非対称は意図どおり**
  （読み出しは壊れないが、書き込みはグローバルを破壊するため）。
- 対象は**実効的な書込先が個別のときだけ**。**ON → OFF の確定は拒否しない**
  （無効パスを抱えたまま「専用をやめる」操作は成功させる＝**主要な復旧手段**）。
  **OFF のままの保存も従来どおりグローバルへ書ける**。

### 【task_05b】完了（2026-08-10）= 無効な個別パスでの保存拒否（枝番タスク）

- `resolve_hotkey_presets_save_target(...) -> (stored_path, status)` を新設。
  `status` = **`individual` / `global` / `invalid`** の 3 値で、
  **`resolve_hotkey_presets_save_path` はその薄いラッパ**（戻り値・既存呼び出し・既存テストは不変）。
  **`invalid` 判定は既存の `resolve_individual_hotkey_presets_path` を再利用**（新規パス判定を書かない）。
  `config_root` 空も `invalid` 側へ混ぜる（分岐を増やさない。通常経路では起きない）。
- `HotkeyPresetsIo.reject_invalid_target()` で理由表示（**現在のパス + 復旧手段**を含む）。
  **モーダルはこのファイルへ集約**（tests_ui の fail-fast ガードがここを見ているため、
  `app.py` に新しい `showerror` を増やさない）。
- `App.save_hotkey_presets` は**書き込み前に** status を見て拒否し、**`data`・dirty を一切変更しない**。
  `dialogs.py` は**無変更**（戻り値 False で閉じない契約が既にある）。
- 読込側（`build_runtime_data_from_split` / `describe_hotkey_presets_source`）は**完全に無変更**。
- 実測: compile clean / `tests` **225**（+3）/ `tests_ui` **200**（+4）/ smoke pass。
- `reviewer` = **採用（完了可・指摘なし）**。

### 【仕様確定 v0.6】無効な個別パスは拒否せず既定パスへ寄せる（2026-08-10・ユーザー確定・**v0.5 を反転**）

- **v0.5【O3】= 拒否**をユーザーが再考し、**却下していた案 B（既定の個別パスへ寄せる）を採用**した。
- 反転の理由（v0.5 で (C) を推した根拠が実際には弱かった）:
  1. **task_05 で保存先ラベルがチェックに追従して表示される**ようになったため、
     リダイレクト先は **OK を押す前に見えている**。「黙って別の場所へ書く」に当たらない。
  2. **ON + パス未設定は既に「既定パスへ新規作成」**（task_04 +【E】）。
     無効パスを「使えるパスが記録されていない」と同一視すれば**同型**になり、新しい概念が増えない。
     むしろ拒否の方が同じ状態に対して分岐を 2 つ持つ。
  3. **キーを書き換えない**という (C) の唯一の利点は、**【K】Import が 2 キーとも書き換える前例**が
     あるため原則として立たない。**【N】の趣旨（再 ON で同じファイルへ戻る）も、
     二度と書けない無効パスの保持には寄与しない**。
  4. 拒否は**復旧手段が JSON の手編集しか無く行き止まり**（GUI ツールとして厳しい）。
  - グローバル保護は両案とも同等（書込先は必ず個別側に取る）。
- **【dirty 規則】`hotkey_presets_path` の値が実際に変化したときだけ dirty を立てる**（同時確定）。
  - 発見の経緯: 「パス書き換えで dirty を立てるか」を検討した際、**現状はフラグ変更（OFF→ON）への
    相乗りで永続化している**だけで、**「最初から ON」の経路には dirty が立たない穴**があると判明。
  - **同値の再確定では立てない**ため、**通常運用（ON + 有効パスで内容編集）は dirty にならない**
    （受入条件 4 を維持）。リダイレクトと既存の穴を**1 規則で塞ぐ**。
- 正本 `spec_detail/` への反映は **task_08**（フェーズ中の正は暫定仕様）。

### 【task_05c】完了（2026-08-10）= 無効パスのリダイレクト + パス変化時 dirty

- **task_05b の拒否経路を全削除**（`resolve_hotkey_presets_save_target` の 3 値ステータス /
  `reject_invalid_target` / 保存側の `invalid` 分岐）→ `resolve_hotkey_presets_save_path` へ復帰。
  **恒久互換レイヤーを残さない**。判定は既存の `resolve_individual_hotkey_presets_path` の再利用のまま。
- 書込先の分岐 = **有効な個別パス → そのパス / 未設定 “または” 無効 → 既定パス / OFF → 空文字**。
  `config_root` 空は従来どおりグローバルへ倒す。
- `App.save_hotkey_presets` は**反映前の値と比較して、変化したときだけ** `set_dirty(True)`。
  **保存失敗時は立てない**（`data` 不変と同じ契約）。フラグ変化による dirty は従来どおり併存
  （`set_dirty(True)` は冪等なので特別扱いしない）。
- `dialogs.py` は `invalid` の文言のみ変更（**これから書く既定パスを必ず含める**）。
  **OFF の表示契約（「グローバル（<パス>）」）と出どころラベルは不変**。
- 実測: compile clean / `tests` **222**（-3）/ `tests_ui` **199**（-1）/ smoke pass
  （**減は拒否系テストの削除・差し替えによる想定内**）。
- `reviewer` = **採用（完了可）**。**参考指摘 1 件**: `dialogs.py` の invalid 文言生成で
  保存先解決を 1 回追加呼び出ししている（軽量・実害なし・非ブロッキング）。

### 【task_06】完了（2026-08-10）= Import の強制 OFF + 別名保存時の複製

- **強制 OFF**: `clear_individual_hotkey_presets(runtime)` を新設（**キーが無くても 2 キーとも設定**。
  `hotkey_presets` の内容には触らない）。呼び出しは `import_config` の
  **`load_legacy_runtime_data` 直後・`apply_global_defaults` 直前の 1 箇所だけ**。
  **`ensure_config_compatibility` へは入れない**（通常読込へ波及させないため。§2【K】）。
- **複製**: `relocate_individual_hotkey_presets(...)` を新設。**`shutil.copyfile` でファイルを複製**し、
  **runtime の内容は書き出さない**（書き手はマネージャ 1 本のまま）。分岐 = OFF → 何もしない /
  コピー元が有効かつ実体あり + コピー先に実体なし → **複製** /
  コピー先に実体あり・コピー元の実体なし・コピー元が config 外・同一パス → **複製せずパスだけ返す**。
  **元ファイルは消さない**。
- **【起票時の判断】複製の起動条件を `save_path != keymap_set_path`（＝保存先そのものの変化）に限定**した。
  `save_as` と通常保存は同じ `save_keymap_set_to` を通るため、「算出した既定パスと記録済みパスの
  不一致」で判定すると、**個別パスを手で `custom.json` 等に設定した構成セットで
  通常保存しただけでも勝手に付け替えが走る**。判定は `keymap_set_path` 更新より**前**に行う。
- 呼び出しは `save_runtime_data` の直前で、返り値を `data["hotkey_presets_path"]` へ反映してから保存
  （**新しい keymap_set の payload に新パスが載る**）。**dirty の扱いは変えない**。
- **【M】同一 stem の無警告共有は既知の制約**として許容（上書き確認を新設しない）。
- 実測: compile clean / `tests` **225**（+3）/ `tests_ui` **202**（+3）/ smoke pass。
- `reviewer` = **採用（完了可）**。**参考指摘 1 件**: 「コピー元とコピー先が同一パス」分岐に
  明示テストが無い（既存の exists 判定でも実質スキップされるため実害なし・非ブロッキング）。

### 【task_07 レビュー】2 本立ての指摘と採否（2026-08-10・ユーザー確定）

`deep-reviewer` + `codex-adversarial-reviewer` を実施。**両者が独立に B1 を検出**
（phase.md の「2 本立てを省略しない」の狙いどおり）。**設計の骨格は両者とも「正しく実現されている」**判定。

| 指摘 | 判定 | 理由 |
|---|---|---|
| **B1** グローバル上書き経路 | **採用（task_07b で是正）** | 下記の独立節 |
| **N2** payload の §5.7 正規化漏れ | **採用（task_07b）** | 受入条件 11 の明確な未達。`trigger_set_path` は通しているのに片方だけ素通し |
| **N5** application 側の未保存ガード | **除外** | **到達経路が残っていない**（Import は task_06 で強制 OFF・新規作成は data ごと差し替わる）。UI にある契約を二重化する価値が薄い |
| **N3** 残置パスの初回 ON で旧ファイルを上書き | **保留 →現状維持 + 明記** | **アプリ由来の値かを値から判別する手段が無い**。v0.7 の既知の制約へ |
| **N4** 複製失敗で本体保存ごと中止 | **保留 →現状維持 + 明記** | 中途半端に成功させると「専用のはずが元と共有」が黙って生まれる |
| **N6** OFF 復帰時にグローバルが読めない | **保留 →現状維持 + 明記** | **読めない = 内容が既に使えない**ので失うものがない。破損上書きは §5.10.4 の既定規則どおりで、本経路が新たに作ったリスクではない |
| **N7** OFF 復帰で編集中の一覧が消える | **除外** | 仕様【H2】どおり。実機目視の観点には含める |
| **N8 / N9** 文書ずれ・台帳の追加 | **採用（task_08）** | §7 の v0.3 記述を直さないと**正本に誤記が入る** |
| **N10** 表示のたびに 2 ファイル実読み | **除外** | 読み取りのみ・小規模 |
| **Codex high②** symlink/junction で containment を抜けられる | **除外** | ユーザー自身の config 配下へのリンク設置が前提で、外部データ由来の経路ではない。`is_path_within` の realpath 化は **canonical identity 比較全体へ波及**し phase 09 のスコープ外 |
| **Codex medium** 正本が旧記述 | **指摘に当たらない** | 暫定仕様先行モードで**昇格は task_08 に計画済み** |

### 【仕様確定 v0.7】グローバル保護の保存先ガード（2026-08-10・ユーザー確定）

- **B1 の内容**: config.json の `hotkey_presets_path` が **`user/hotkey_presets/` 直下**を指し、
  その名前が構成セットの stem と一致すると、**個別の保存先とグローバルの読み先が同一ファイル**になる。
  結果は一度きりの損失ではなく**継続的なエイリアス**で、**専用側の編集が全構成セットのグローバルへ
  漏れ続ける**（ユーザーは専用のコピーを持っているつもり）。
  - **到達には config.json の手編集が必要**。`build_startup_payload` は
    **既存キーを引き継ぐだけでキーを増やさない**ため、アプリがこの値を作ることはない。
    ただし**一度書かれると round-trip で永久に保持**され、状態が自然に解消しない。
  - 衝突相手になりやすいのが `user/hotkey_presets/default.json`（旧既定）と `default.json`
    （既定の構成セット名）という**システム内で最もありふれた名前の組**。
- **ユーザー案 = 「個別を `global/` 内へ保存しようとしたら警告」**。これは**予約領域の汚染**を
  防ぐが、**B1 本命（保存先が `global/` の外）を捕まえない**。→ **2 条件の併用で決着**。
- **判断根拠**: 関門（書き込み直前の保存先チェック）は予約ディレクトリ規則で**どうせ作る**ので、
  同一ファイル検出の**追加コストは比較 1 個**。「リスクに見合うか」ではなく
  「**同じ関門を半分だけ実装して残りを開ける理由があるか**」の問題として、閉じる方を選んだ。
- **【O4】= `global/` 配下 または グローバルの読み先と同一 なら拒否**（理由 2 種で文言を分け、
  同一ファイル側は**移行手順②への誘導**）。**【O3】のリダイレクトより後段**の最終関門。
  **ON → OFF の確定と OFF のままの保存は対象外**（復旧経路・通常経路を塞がない）。
- 併せて **【G】に `global/` の予約規定**を明文化し、**未定義だった 3 挙動（N3 / N4 / N6）を
  既知の制約として明記**、**§3-1 に payload 正規化**、**受入条件 18** を追加。

### 【task_07b】完了（2026-08-10）= 保存先ガード + payload 正規化

- `individual_hotkey_presets_save_rejection_reason`（application・`reserved_dir` / `global_conflict`）。
  判定は **`is_path_within` / `canonical_path` の再利用**で、**`canonical_path` は比較専用に留めた**
  （保存値・表示値へ混ぜない）。`"global"` は **`HOTKEY_PRESETS_RELATIVE_PATH` の dirname から導出**。
  **`resolve_hotkey_presets_save_path` の契約は不変**（task_05c で畳んだ 3 値タプルを再導入していない）。
- presentation: `hotkey_presets_io.show_save_path_rejection`（**モーダルはこのファイルへ集約**）。
  `app.py` は **`write_presets` の前**に判定して `False`（**`data`・dirty は完全に不変**）。
  `dialogs.py` は無変更（戻り値 False で閉じない契約が既にある）。
- `split_payloads` の `hotkey_presets_path` を `to_config_relative_or_absolute` へ
  （**OFF の空文字は空文字のまま・キー順不変**）。
- 実測: compile clean / `tests` **229**（+4）/ `tests_ui` **206**（+4）/ smoke pass。
  **サブエージェントがセッション上限で失敗したため実測はメインが直接実行**。
- `reviewer` = **採用（完了可）**。**参考指摘 2 件**（`split_payloads` の二重評価 /
  `show_save_path_rejection` が理由 2 値以外を一律 `global_conflict` 扱い。どちらも非ブロッキング）。

### 【運用インシデント】Codex ジョブ復旧時のプロセス誤終了（2026-08-10）

- 実装フォワーダがハングした Codex ジョブの復旧中に **`taskkill /PID <pid> /T /F`** を実行し、
  **PID 再利用により無関係な `node_repl` プロセス約 22 個**を子プロセスと誤認して終了させた。
- リポジトリのファイルへの影響なし（差分は task_04 の想定どおり）。他ジョブの state も無事。
- **再発防止**: **詰まったジョブに `taskkill /T` を使わない**。
  `instructions/common/rules_detail/codex_operations.md` **§4 の state 手修復**
  （backup → `cancelled` へ書換・`.log` は保全）に倒す。`session.md` の resume_hints にも明記した。

### 【仕様確定 v0.8】実機目視の 3 指摘（2026-08-15・ユーザー確定）

task_07 の実機目視（項目 2 / 7 / 10）でユーザーが検出。**3 件とも実装バグではなく確定仕様どおりの
挙動**だったが、実運用で破壊的なため**仕様側を改訂**した（暫定仕様 08 v0.7 → **v0.8**）。

- **①【§3-5】残置パスも落とす**（項目 2）。**発現の実物証拠** = `config/user/keymap_sets/keymap_set1〜3.json`
  （08/02〜08/05 作成）に `hotkey_presets_individual` **キーが無く**、`hotkey_presets_path` が
  **旧グローバル `user/hotkey_presets/default.json`** のまま残っていた。v0.7 までは
  **フラグだけ OFF に倒してパスは runtime へ載せていた**ため、**初回 ON + OK の保存先が残置パス**になり、
  ①stem 由来の名前にならない ②同じ残置パスを持つ 3 セットが 1 ファイルを無警告共有（【M】が既定で発生）
  ③`global/` 配下でも現行グローバルと同一でもないので**【O4】も素通り**、の 3 点が同時に起きた。
  → **フラグキーが無ければパスも未設定として扱う**（判定はキーの有無。**false でもキーがあれば保持**して
  【N】の再 ON 復帰を壊さない）。**v0.7 の既知の制約「残置パスの初回 ON」は撤回**。
- **②【I】トグル時の読み直しを再採用**（項目 7・**v0.4 の【I 撤回】を反転**）。
  v0.7 までは一覧を保持していたため、**OFF → ON でグローバルの内容が個別ファイルへ上書き**された。
  原因は「チェックは保存先の選択なのに、書かれる内容は直前に表示していた**別ファイル**の内容」という非対称。
  → **切替先から読み直す**。**編集済みなら破棄確認**（キャンセルでチェックを戻す）。
  **【E】は「個別ファイルが読めないときだけ引き継ぐ」へ縮小**。
  **【H2】撤回 = OFF での OK もグローバルへ書く**（読み直し済みなので書かれる内容はグローバル自身）。
  **読み直しに失敗した場合だけ書かない**（【H2】の狙い＝流出防止はここへ移した）。
  **runtime への反映は OK 時の 1 本のまま**（プリセット単独の注入 API は増やさない）。
- **③【O2】config 外パスは読み出しでは有効**（項目 10）。ユーザーの認識「読み出すが保存は管理下に新規作成」
  を採用。**書き込みは【O3】のまま管理下の既定パスへ寄せる**ため、**外部パスは初回に内容を拾うための
  移行導線**になり、次の保存で `hotkey_presets_path` が管理下の値へ置き換わる（v0.6 の dirty 規則）。
  【O】の「config 配下限定」は**書き込み側の制約**に限定。UI の「無効」表示は廃止。
- 受入条件 **3 / 13 / 15** を改訂。§4 却下記録に **I / O / §3-5** の 3 行を追加・更新。

### 【v0.8 の敵対的レビュー】指摘 5 件の採否（2026-08-15）

`codex-adversarial-reviewer` = **needs-attention（High 2 / Medium 3）**。対象は v0.8 の仕様差分のみ。

- **High 1 = 採用（ユーザー判断）**: 「グローバルが読めない時に OFF へ戻す」経路で、**その OK を止めても
  閉じて開き直せば通常の OFF 経路になり、個別由来の内容がグローバルへ書かれ得る**。
  → **ユーザー確定 = 読めなければ一覧を「組込既定」へ差し替え、OK でグローバルを作り直す**。
  読み出し規則（読めなければ置き換えない＝組込既定のまま）と**同じ状態**になるため、
  トグル直後でも再オープン後でも結果が一致し、**個別の内容が OFF 状態へ持ち越されない**。
  v0.8 草案の「維持して書かない」は §4 却下記録 I 行 (D) へ落とした。
- **High 2 = 修正して採用（文書のみ）**: 受入条件 15 の「保存後は管理下の値へ置き換わる」は
  **個別 ON での保存に限る**（OFF 保存は書込先がグローバルなので【O3】が発火せず、外部パスは【N】で保持）。
  → 受入条件 15 と【O2】へ区別を明記。**挙動は変えない**（再 ON 時に【O3】で寄せられるため）。
- **Medium 3 = 除外**: 「フラグキーの有無をアプリ由来の証明とするのは脆弱」。**既知の制約として明記済み**
  （フラグキーを持つ keymap_set の `hotkey_presets_path` 手編集は記録どおり使う）。
  値のブラックリスト化は §4 で却下済み。
- **Medium 4 = 修正して採用（文書のみ）**: config 外パスの同期読み出しは到達不能な UNC 等で待たされ得る
  → **既知の制約として 1 行明記**。非同期化はしない（trigger_set・keymap の読み出しと同じ性質のため）。
- **Medium 5 = 修正して採用（タスク定義側）**: 破棄確認モーダルの追加時は
  **tests_ui 4 ファイルの fail-fast ガードを更新する**（`session.md` の resume_hints 既出）。
  仕様書ではなく**トグル実装タスクのチェック項目**として持たせる。

### 【実機目視 OK】v0.8 分の 16 項目（2026-08-15・ユーザー報告）

- task_07 §3 の表（項目 1〜16）を **v0.8 の期待値で通し確認し OK**。以後のやり直しは
  **v0.9 で追加した 4 項目（13 / 17 / 18 / 19）に限定**する。

### 【task_07 フェーズ完了判定前レビュー】v0.8 分の 2 本立て（2026-08-15）

対象は **`b6ca866..HEAD`**（v0.8 の是正 3 タスク）。2026-08-10 の 2 本立ては task_01〜07b が対象で、
その後に入った 07c/07d/07e は横断レビュー未実施だったため追加で実施した。

| 指摘 | 判定 | 理由 |
|---|---|---|
| **Codex High** symlink/junction で保存先ガードを回避 | **除外** | 2026-08-10「Codex high②」と**同一指摘**。ユーザー自身が config 配下にリンクを置く前提で、外部データ由来の経路ではない。`is_path_within` の realpath 化は canonical identity 比較全体へ波及し phase 09 のスコープ外。**v0.8 の 3 改訂はこの判断を変える要素を持ち込んでいない** |
| **deep 指摘1（中）** 未記録パスでの初回 ON が既定保存先の実体を上書き | **採用（task_07f）** | 下記の独立節 |
| **deep 指摘2（中）** OFF で開いた直後の OK が個別由来の一覧をグローバルへ書く | **採用（task_07f）** | 同上 |
| **deep 指摘3** 破棄確認の条件が仕様より緩い（`and self._temp != replacement`）| **除外** | 挙動は安全側（データ損失なし）|
| **deep 指摘4** 到達不能な `external_missing` × OFF 分岐がテスト付きで残存 | **候補送り** | 削除は task 範囲外 |
| **deep 指摘5 / 6** application API の非対称・組込既定の取得点 | **候補送り** | 参考指摘 |
| **deep 指摘7** 正本昇格時の文言整合（`invalid` 表示廃止 / OFF 復帰の供給点 / §5.10.4）| **修正して採用（task_08）** | 正本へ誤記を持ち込まないため |
| **deep 指摘8** `config_root` 空のときの表示 | **除外** | 実アプリでは必ず非空（`app.py:57`）|

### 【仕様確定 v0.9】読み直しの穴 2 件（2026-08-15・ユーザー確定）

**根因は共通**: v0.8 の「切替先を読んでから書く」という保証が**トグル経路にしか無い**。

- **①【S】個別へ書く直前の上書き確認**（指摘1）。**発現の実例（ユーザーが特定）** =
  **別名保存で既存の構成セットを上書き**すると、上書きされた側の専用プリセットファイルが
  参照者を失って残り、**そのセットで個別を ON にして OK した瞬間に潰れる**（記録パスが空なので
  読み直しが起きず、書込先だけ stem から導かれる）。stem 衝突や手編集を要さず**通常の UI 操作だけで到達する**。
  - **ユーザー選択 = 「読み直して差し替える」ではなく「上書き確認を出す」**。理由 = 表示中の一覧が
    黙って入れ替わる方が驚きが大きく、**【E】の「引き継いでコピーを作る」導線を潰す**ため。
    **通常の ON/OFF では出さない**ことが条件。
  - **③の判定基準は敵対的レビュー High を受けて変更**: 当初案「保存先 ≠ 表示中の一覧の読み出し元」は、
    **別名保存【L】でコピー先の実体を採用すると**パスだけ新しくなり一覧は旧ファイル由来のまま残るため
    **一致して素通りする**。→ **「保存先の実体の内容 ≠ ダイアログが読み込んだ一覧」（読めない場合も含む）**
    へ変更した。出どころの追跡（runtime への内部キー追加）が不要になる副次効果もある。
  - **別名保存でコピー先を共有した場合に旧内容が表示されること自体は許容**（ユーザー判断。
    **手編集された config 外パスを読むのと同じ扱い**）。**保存先は管理下のまま**で、【S】が損失を止める。
- **②【I2】OFF で開いた時点の一覧確定**（指摘2）。到達 = **グローバル欠損 + 個別 ON セットの Import**
  （Import は【K】で強制 OFF にするが、**グローバルが読めなければ `apply_global_defaults` は置き換えない**）。
  ラベルは「組込既定を表示中」なのに **runtime の個別由来の一覧がグローバルとして書かれた**。
  **ON は対象外**（【E】維持）。
- 敵対的レビューの残り: **原子性（確認と書き込みの間の競合）= 除外**（§5.8.3 等と同水準の既知の性質）/
  **正本 §5.10.4 との優先関係 = 修正して採用（文書のみ・task_08 で明記）**。

### 【task_07f】完了（2026-08-15）= 上書き確認 + OFF 開時の一覧確定

- application: `individual_hotkey_presets_overwrite_conflict`（`split_loading` + `ConfigService` 委譲）。
  **実体なし / `loaded_presets=None` → False**、**読めない → True**、**内容不一致 → True**。
  比較は**正規化後どうし**（`canonical_path` は使わない＝【O4】と役割を混ぜない）。
- presentation: `app.py` が **【O3】→【O4】→【S】→ `write_presets`** の順で判定し、
  **「いいえ」は `write_presets` へ進まず `False`**（`data`・dirty・ファイル不変）。
  モーダルは `hotkey_presets_io` へ集約。`dialogs.py` は **OFF のときだけ**初期化で一覧を確定し、
  `on_ok` は **`_loaded_temp`**（編集後の `_temp` ではない）を渡す。
- 実測: compile clean / `tests` **242**（+5）/ `tests_ui` **220**（+7）/ smoke pass。
- `reviewer` = **完了可**（差し戻し無し）。参考指摘 1 件（「はい」分岐に data/dirty の明示 assert が無い）。
  **fail-fast ガードは他 3 ファイルが `tkinter.messagebox` を直接パッチしており更新不要**と確認。

### 【仕様確定 v0.10】上書き確認の 3 択化（2026-08-15・ユーザー確定）

**実機目視 項目 17 でユーザーが検出**: v0.9 の 2 択（上書きする / いいえ）では
**既存ファイルの内容を採る手段が UI に無い**。記録パスが空だと【I】のトグル読み直しが空振りし
（読み出しは**記録パス**のみを見る）、**`hotkey_presets_path` が記録されるのはプリセットの保存が
成功した後**なので、確認を断り続ける限りその実体には二度と到達できない
＝ **「上書きする」か「専用化を諦める」の 2 択**しか残らなかった。

- **【S】を 3 択へ**（**上書きする / 既存を読み込む / キャンセル**）。
  **「既存を読み込む」は一覧と `_loaded_temp`（破棄確認・【S】判定の比較基準）を差し替えるだけで、
  保存せずダイアログも閉じない**。差し替え後に改めて OK すると内容が一致するため確認が出ず、
  **既存の内容が保たれたまま保存が通る**。**明示選択なので追加の破棄確認は出さない**。
- **保存先が読めない（破損）ときは「既存を読み込む」を出さない**（読む対象が無いため 2 択へ縮退）。
- **【S】の発火条件そのものは v0.9 のまま**（実体なし / 比較基準なし → 確認なし、破損 → あり、
  内容不一致 → あり）。受入条件 **21** を追加、19 は文言のみ改訂。
- **却下**: ①**トグル ON の時点で「読み込むか引き継ぐか」を尋ねる**（選択が編集前に来る利点はあるが、
  引き継ぎを選ぶと OK 時に【S】も出て確認が 2 回続く）②**両方入れる**（モーダルが増える）。

### 【v0.10 の敵対的レビュー】指摘 3 件の採否（2026-08-15）

`codex-adversarial-reviewer` = **needs-attention（High 2 / Medium 1）**。

| 指摘 | 判定 | 理由 |
|---|---|---|
| **High 1** adopt 後の再 OK が無確認で通る前提は、別プロセスが保存先を変更・削除すると崩れる | **修正して採用（文書のみ）** | **受入条件 21 へ「一覧を編集せず、保存先が外部から変更されていない場合」と前提を明記**。競合そのものは **v0.9 で原子性として除外済み**の性質と同族で、実装は変えない |
| **High 2** ネストしたモーダルを閉じても親（マネージャ）の grab が復元されず、開いたままメインを操作できる | **除外 → idea 起票** | **既存の「追加」「編集」（`PresetDialog`）が同じ挙動**でアプリ全体の課題。**新経路だけ直すと挙動が不揃い**になるため task_07g では対象外とし、**[idea_10](../../../instructions/backlog/idea_10_nested_modal_grab_restore.md)** へ分離（ユーザー判断）。実害の想定 = モードレスな隙に別の構成セットを読み込むと「書込先は今のセット / 内容は前のセット」の食い違いが起き得る（**内容比較の【S】では捕まらない**） |
| **Medium** レビュー時点の状態表記が「ユーザー確定済」のまま | **除外** | 3 択の採否は**レビュー前にユーザー確定済**で、レビュー対象は条項の文言。版履歴に経緯を明記した |

### 【task_07g】完了（2026-08-15）= 上書き確認の 3 択化

- application: 判定 API を **`conflict` + `existing`（正規化後）を同時に返す形へ置換**
  （**旧 bool API は残さない**＝互換レイヤー禁止）。**同じファイルを 2 回読まない**ため。
- presentation: 3 択は **`messagebox` ではなく自作 `Toplevel`**（`hotkey_presets_io`）。
  `app.py` は**衝突時ハンドラ**を受け取り、**adopt / cancel とも `write_presets` へ進まず `False`**。
  一覧の差し替えは**ダイアログ側**（`_temp` と `_loaded_temp` の両方）。
- 実測: compile clean / `tests` **238**（242 → **-4**。**退行ではなく**、判定 API の単体 5 本が
  戻り値の変更に伴い **5 条件を 1 本にまとめたテストへ置換**された）/ `tests_ui` **223**（+3・ハングなし）/
  smoke pass。
- `reviewer` = **完了可**。指摘 1 件（**破損時も「内容が異なります」と出る**）は**メインで文言を出し分け**に修正。
- **【運用】フォワーダが最終出力を返さず完了通知だけ来た**（ワーカーは exit 0 で完走）。
  `SendMessage` で同じフォワーダを再開して回収した。

### 【実機目視 OK】v0.9 / v0.10 分の 4 項目（2026-08-16・ユーザー報告）

- 対象 = `task_07_integration_check.md` §3 の **項目 13 / 17 / 18 / 19**
  （13 = 別名保存の複製〔17 の前提操作〕/ 17 = **上書き確認の 3 択**〔キャンセル = 不変 /
  既存を読み込む = 一覧が既存へ差し替わり、続く OK が無確認で通って既存の内容が保たれる〕/
  18 = 通常 ON 編集・初回 ON・OFF の OK で**確認が出ない** /
  19 = グローバル削除 + 個別 ON セットの Import → OFF のまま開くと**組込既定に揃い、
  OK で組込既定が書かれる**）。**全項目 OK**。
- **v0.8 分（項目 1〜16）は 2026-08-15 に OK 済**のため、§3 の表は全項目クリア。

### 【task_07】完了（2026-08-16）= 統合確認

- 完了条件 3 つを充足: **§1 自動確認**（task_07g 時点の実測 = compile clean / `tests` 238 /
  `tests_ui` 223 / smoke pass）/ **§2 二次レビュー 2 本立て**（`deep-reviewer` +
  `codex-adversarial-reviewer`。指摘は **v0.9 / v0.10 の仕様確定**と **idea_10 の起票**で決着）/
  **§3 実機目視**（2026-08-15 + 2026-08-16）。
- **受入条件 1〜21 に未確認の項目なし**（自動テスト または 実機目視のいずれかで確認）。

### 【除外・分離した指摘の最終状態】

- **ネストしたモーダルの grab が復元されない** → **除外し [idea_10](../../../instructions/backlog/idea_10_nested_modal_grab_restore.md) へ分離**。
  **既存の「追加」「編集」も同じ挙動**＝アプリ全体の課題であり、新しいダイアログにだけ
  復元処理を書くと挙動が不揃いになるため。**本フェーズでは grab_set を足していない**。
- **確認と書き込みの間の競合（原子性）** → **除外**（§5.8.3 等と同水準の既知の性質として
  正本 §5.10.4 へ明記）。
- **レビュー時点の状態表記** → **除外**。
- **task_05b は task_05c で置き換わった**（v0.5 の拒否 → v0.6 の寄せ）。
  タスク定義は履歴として残し、**実装は 05c が正**。

### 【task_08】完了（2026-08-16）= 正本反映（最終）

- 正本へ昇格: `data_schema.md` **§5.10**（全面改訂。2 系統・移行規則・書き込み前の判定 3 段
  〔寄せ → 拒否 → 上書き確認〕・dirty 規則・別名保存の複製・**UI 契約**・既知の制約）/
  **§5.10.4**（【I2】の例外と【S】を追記）/ **§5.8.8**（**P1 = 表示用の再読込**を台帳へ追加・
  **プリセット単独の注入 API は増やさない**・E5 の強制 OFF）/ **§5.5**（個別指定時は参照する）/
  **§5.4**（子ファイルにはしないが別名保存では複製する）/ **§5.1**（生成停止キーは無くなった）/
  `codebase_map.md`（解決点・保存先算出は application・3 択モーダルは presentation）。
- **§5.10 の見出しのみ「hotkey プリセットの全体ライブラリ」→「〜の全体ライブラリと個別指定」へ変更**
  （§5.9 と同型にするため。**節番号 5.10 / 5.10.1〜5.10.4 は不変**）。
  新設した規定は既存の 4 小節の中に収め、**小節を増やしていない**（参照の安定を優先）。
- 暫定仕様 08 を**凍結**。判断履歴を本ファイルへ集約し、`decisions.md` は索引 1 行のみに戻した。

### 【/refactor_check】判定 = **リファクタ推奨**（2026-08-16）

- 対象 = `39140c5..HEAD -- keyseq/`（**9 ファイル・+708 / -17**）。メトリクス収集は `verifier`。
- 該当: **M1**（`dialogs.py` 1016 行 / +195・`config_service/__init__.py` 734 行 / +135）/
  **M2**（`PresetManagerDialog.__init__` が 99 行）/
  **M6**（`__init__.py:435` の `"user","hotkey_presets","global"` 直値が
  `HOTKEY_PRESETS_RELATIVE_PATH` と同値。他 2 箇所は定数由来で**ここだけ非対称**）。
  非該当: M3 / M4 / M5（申し送りコメント 0 件）。
- 提案書 = [07_refactor_per_keymap_set_presets](../../../instructions/modified_proposal/07_refactor_per_keymap_set_presets.md)
  （**未承認**）。項目 = ①`PresetManagerDialog.__init__` の UI 構築抽出 ②`dialogs.py` の
  `dialogs/` パッケージ化 ③M6 の直値を定数由来へ。
- **`config_service/__init__.py`（734 行）は上位 3 項目から外して候補送り**。
  テストが `patch("keyseq.application.config_service.os.path", ntpath)` で名前空間を差し替えるため
  **本体とパス基盤メソッドを動かせない**制約があり、分割方針の設計判断が別途必要（効果に対しリスクが高い）。
- 退行確認（同時実測）: compile clean / `tests` **238** / `tests_ui` **223** / smoke pass。

### 【task_08 のレビュー】2 本立ての指摘と採否（2026-08-16・ユーザー確定）

**`codex-adversarial-reviewer` = needs-attention（High 3）**:
- ①**§5.10.2「読めた場合はどの経路でも一致する」が個別指定の導入で成立しない** → **修正して採用**
  （phase 08 由来の文の改訂漏れ。「**同じ供給元を読む経路どうしでは一致**」へ限定し、
  **L1〜L3 と E1〜E5 は一致しないのが意図どおり**と明記）。`deep-reviewer` H1 と**同一指摘**。
- ②**adopt 後の再 OK を無条件で通す書き方**（v0.10 受入条件 21 の「外部変更なし」条件が落ちていた）
  → **修正して採用**（**判定は OK のたびに再評価**・外部の変更 / 削除 / 破損なら再び確認・
  **差し替え後に編集した場合は確認なしで編集内容が書かれる**、を明記）。
- ③**別名保存で複製成功後に keymap_set 保存が失敗した場合が未定義**（実装も補償なし）
  → **既知の制約として明記 + [idea_11](../../../instructions/backlog/idea_11_save_as_preset_copy_rollback.md) 起票（優先度低）**。
  **ユーザー確定**。実装は変更しない。
  - 補足調査: ディスク上の keymap_set は旧パスのままなので**次回起動の読み込み先は変わらない**が、
    **同一セッションで再編集すると【S】の確認が発火しない**（複製の内容 = 読み込んだ一覧のため
    内容比較が一致する）ので**無警告で複製側へ書かれる**。keymap_set を保存し直せば解消し、
    **データ自体は失われない**（複製側に残る）。前提が例外系のため優先度低で後送り。

**`deep-reviewer` = 修正要（差戻しではない）**。採否:
- **H1**（§5.10.2 の矛盾）= Codex ① と同一 → **修正して採用**。
- **H2**（**「個別 ON かつ記録パスが未設定 / 空」の保存先が正本から導けない**。判定 1 が
  「config 配下でない場合」しか書いておらず、**初回 ON の主経路**〔受入条件 3〕が落ちていた）
  → **修正して採用**（判定 1 を「**未設定 / 空 / 非文字列 / config 外**なら既定パスへ」へ）。
- **M3**（複製元が config 外なら複製しない）/ **M4**（**OFF での別名保存は追随しない**ため
  再 ON で旧 stem のファイルへ書き共有に入り得る）→ いずれも **修正して採用**（文書のみ・実装不変）。
- **M5**（**adopt 後に「一覧の出どころ」ラベルが追従しない**。表示分類に
  「保存先の実体を採用中」が無い）→ **実装変更なし + 1 行明記**（**ユーザー確定**）。
  根拠 = **個別指定のチェックと保存先の表示で対象ファイルは判別できる**ため、状態を増やさない。
- **M6**（§5.8.8 の P1 が OFF 方向のみ / 台帳の定義と噛み合わない）→ **修正して採用**
  （**両方向のトグル + OFF で開いた時点**へ広げ、**台帳の本体ではない**旨の注記を追加）。
- **M7**（`current.md` の提案書 次採番が未更新）→ **採用**（07 起票済・次は 08 へ修正）。
- **M8**（保存先の表示と一覧の出どころを 1 見出しに混ぜている）→ **修正して採用**（2 つに分けた）。
- **L9**（§5.1 への経緯の混入）/ **L11**（【S】が発火する側の列挙）/ **L12**（同期読み出しで
  UI が待つ制約の未昇格）/ **L13**（`load_individual_hotkey_presets` が codebase_map に無い）/
  **L15**（`current.md` の提案書一覧の陳腐化）/ **L16**（task_08 の文言と `/refactor_check` の齟齬）
  → **すべて修正して採用**。
- **L10**（§5.10 の**見出しのみ変更**したのは task_08 の「見出しを変更しない」に反する）
  → **採用可**（§5.9 と同型化する意図が妥当・**旧見出し文言への参照は 0 件**・節番号は不変）。
  **齟齬は本アーカイブに記録**して決着とする。
- **L14**（`config_root` が空だと個別 ON でもグローバルへ書く）→ **除外**
  （production では到達不能。形式上の矛盾に留まる）。
