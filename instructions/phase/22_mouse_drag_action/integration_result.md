# integration_result（phase 22: マウスのドラッグ操作）

対象差分: `0518c93..HEAD`（task_01 = `bae9cd8` / task_02 = `9f22e3c` / task_03b = `88ee969`）+ 本タスクの正本昇格差分。

## 統合確認（verifier・.venv python）

| 項目 | 結果（task_03 時点 → task_03b 後） |
|---|---|
| `-m compileall -q keyseq main.py tests tests_ui` | clean → clean |
| `-m unittest discover -s tests` | 478 ran OK → **479 ran OK**（skipped 7） |
| `-m unittest discover -s tests_ui` | 445 ran OK → **446 ran OK** |
| `-m tests.smoke_app` | SMOKE OK → SMOKE OK |

## 二次レビュー（task_01 + task_02 の差分）

- `codex-reviewer`: 指摘なし。
- `deep-reviewer`: 差分は完了可（フェーズ完了は task_03 の残作業のため時期尚早）。ユーザー採否 =
  - **採用 → task_03b**: 指摘 1（ドラッグ 4 キーの永続化を検証するテストが無い）/ 指摘 2（種別を切り替えて戻す往復のテストが無い）/
    指摘 4（`drag_speed` に NaN を渡すと比較が常に偽になり既定へ倒れない）。
  - **採用 → 実機目視に 9 項目目を追加**（ドラッグ ON/OFF でダイアログが伸縮し速度欄と OK が隠れない）。
  - **記録のみ 4 件**: ①`click_mouse` 側が `FAILSAFE` に触らないことの検出力が弱い ②速度欄の `int()` 判定は `"1000.5"` を弾く
    ③座標取得の排他は `instate` ガードを持たない ④`ActionDialog` の座標取得リスナーはダイアログ破棄後の `after(0, ...)` で `TclError` になりうる
    （**task_02 以前からの既存挙動**・「別タスク化候補」へ送り）。
- task_03b の `reviewer`: 採用（`not (speed > 0)` への 1 行修正 + テスト 3 種。変異検査で狙ったテストのみ失敗し復元確認）。

## 実機目視（ユーザー・2026-09-19）

**全 9 項目 OK**（暫定仕様 19 §8-11 の 8 項目 + 二次レビュー採用で追加した 9 項目目）。

1. メモ帳等で文字列の範囲選択（短距離 50px / 長距離 500px の両方） … OK
2. エクスプローラ等でドラッグ&ドロップ … OK
3. 速度を変えると移動の速さが変わる（100px 超で比較） … OK
4. 既存の単発クリックのアクションが従来どおり … OK
5. 最長（5 秒クランプ）のドラッグ後に停止キー・トグルが正常に効く … OK
6. 離す点を画面の隅に指定したドラッグが完了し、ボタンが押しっぱなしにならない … OK
7. ドラッグ中に物理マウスを画面の隅へ動かしても中断せず、終了後にボタンが離れている … OK
   （**観察**: 物理マウスを動かすと離す位置は指定どおりにならず、所要時間の経過後に離される。
   物理入力との競合は仕様で制御しておらず、項目⑦の判定条件は「中断しない」「終了後に離れている」の 2 点。
   この観察を正本 §5.11.5 の既知の制約へ 1 行追記した）
8. ドラッグ以外の (0,0) クリックでは FailSafe が従来どおりエラーになる … OK
9. （追加）ドラッグ ON/OFF でダイアログが伸縮し、速度欄と OK ボタンが隠れない … OK

## リファクタ判定（`/refactor_check`）

**不要**（M1〜M6 該当なし。PHASE_BASE = `0518c93`・対象 = ソース 4 ファイル）。

| 記号 | 測定値 |
|---|---|
| M1 | 最大は `presentation/dialogs/action_dialog.py` 418 行（PHASE_BASE 340 行・+78）。600 行閾値に未達。他は `action_executor.py` 165（+29）/ `input_gateway.py` 145（+15）/ `domain/config.py` 342（+7） |
| M2 | 新規追加関数の最大は `_build_drag_ui` 約 28 行・`_get_drag_fields` 約 22 行。80 行超の新規発生なし（`ActionDialog.__init__` は 104 → 113 行の既存巨大関数で、本フェーズの変更は 40 行未満） |
| M3 | ドラッグ UI の表示切替は `_sync_drag_ui` 1 箇所に集約。同型ブロックの 3 個目は発生せず |
| M4 | 対象なし（列挙型ボイラープレートの増加なし） |
| M5 | 申し送りコメントの新規追加なし（`git diff 0518c93..HEAD -- keyseq main.py` に該当行 0 件） |
| M6 | 既存定数と同値の直値なし（`DEFAULT_DRAG_SPEED_PX_PER_SEC` / `MIN_DRAG_DURATION_SEC` / `MAX_DRAG_DURATION_SEC` を定数化して参照） |

## 正本への昇格（本タスク）

- `spec_detail/data_schema.md` に **§5.11「アクション要素」を新設**（5.11.1 種別ごとのキー / 5.11.2 クリック /
  5.11.3 ドラッグ / 5.11.4 実行規定 / 5.11.5 既知の制約）。§5.2・§5.6・§5.6「旧形式互換」から参照行を追加。
  §5.6 の下にはぶら下げていない。分量は 5.11 全体で約 60 行のため親ファイルへ実体を置いた（子分割はしない）。
- 明文化した事項: マウスのボタンも例外時に必ず離す / ドラッグ中は pyautogui の FailSafe を無効化する（理由と失うもの = 四隅の緊急停止）/
  マウス操作は send guard の対象外 / 物理マウスとの競合で離す位置がずれる。
- `codebase_map.md`: 「キーの送信」節の隣に**マウス操作**節を追加（`click_mouse` / `drag_mouse`・所要時間の算出は application 層・`ActionDialog` のドラッグ UI）。
- 暫定仕様 19 を**凍結**（ヘッダを「凍結済・v0.5（2026-09-19・phase 22 完了・正本反映済）」へ）。

## 完了判定前レビュー

- `codex-adversarial-reviewer`: **approve（指摘なし）**。例外時の解放・`FAILSAFE` の退避と復元・`drag` false の生成停止を静的に確認。
  限界として「テスト実行と実機動作は未検証」を明記（Codex は python を実行できないため。実測は verifier と実機目視が担当済）。
- `deep-reviewer`: **修正要**（**文書側のみ。コード修正は不要**）。5 観点のうち仕様適合性（正本の文言）とチェック漏れが NG、
  依存方向・責務分離・不要変更は OK。ユーザー採否（2026-09-19）=
  - **採用 → 正本の修正**（本タスクで実施・実測で裏取り済）:
    1. **§5.1 と §5.11.3 の矛盾**（§5.1 が「生成を止めているキーは無い」と断定したまま）→ §5.1 に条件付き出力を追記。
    2. **§5.11.4 の手順に「掴む点への移動」が無い**（実装は `moveTo(x, y)` が先頭）→ 4 手順へ修正。
    3. **`clicks` は保存値も 1**（`_get_drag_fields`）→「実行・保存とも 1」へ。
    4. click 側の `x` / `y` 不正時の挙動（エラー報告のみで何も送らない）を §5.11.2 へ追記。
    5. `codebase_map.md` の「マウス（`pyautogui.click`）は対象外」→ ドラッグを含む表現へ修正 + send guard の用語補足・区切り線の体裁。
  - **採用 → 正本の限定 + idea 起票**: 個別 sequence JSON の単体読込（`_normalize_sequence_payload`）が
    `actions` の正規化を通らない（**phase 22 以前からの既存の穴**・実測確認済）→ §5.11 冒頭を経路限定し
    **【実装未追従】**として明記、[idea_24](../../backlog/idea_24_sequence_payload_action_normalization.md) を起票。実装は変更しない。
  - **採用 → 正本の限定 + 別タスク化候補**: `button` が非文字列だと `action_executor.py:119` で `AttributeError`
    （`.strip()` が try の外）→ §5.11.2 を「文字列で」と限定し、1 行修正は `current.md`「別タスク化候補」へ。
  - **採用 → 正本へ明記**: `drag_speed` は UI が `int()` / 実行が `float()` で `1000.5` の扱いが割れる →
    「保存は整数・実行は正の実数も受け入れる」を §5.11.3 の表へ追記。
  - **記録のみ（別タスク化候補へ）**: ①初回移動の失敗時に `mouseUp` が呼ばれないことをテストで固定していない
    ②ドラッグ 4 キーのファイル層での永続化往復テストが無い ③`to_x` 欠落時の一覧表示が中途半端（実行はエラーになる）
    ④`features.md` §4.2 から §5.11 への参照が無い。
  - **確認済み（問題なし）**: `codebase_map.md`「マウス操作」節の事実主張は実測と一致 / FailSafe の復元は二重 `finally` で例外時も成立 /
    クランプ境界と NaN のテストは検出力あり / 後方互換（`drag` 無し・`False`）を検証済 / 記録物は揃っている。
- task_03 の `reviewer`（記録内容と正本反映の整合確認）: **採用 / 完了可**（指摘なし）。
  §5.11 の記述を `action_executor.py:111-156` / `input_gateway.py:129-145` / `action_dialog.py:158-228` /
  `domain/config.py:8,311-321` と対照し、数値・キー名・既定値・エラー時挙動の一致を裏取り済み。
