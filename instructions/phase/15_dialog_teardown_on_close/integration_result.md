# phase 15 統合結果（task_04）

対象: `ecdb3eb..HEAD` の phase 15 全差分（task_01 / task_02 / task_03）+ 本タスクの是正 3 件。
判定と根拠のみを記録する（生ログは載せない）。

## 1. 統合確認（`verifier` 実測・`.venv` の python）

| 項目 | 初回 | 是正後 |
|---|---|---|
| `compileall` | clean | clean |
| `unittest discover -s tests` | pass 417 / skip 7 | pass 417 / skip 7 |
| `unittest discover -s tests_ui` | pass 320 | **pass 321** |
| `tests.smoke_app` | pass | pass |
| 単独実行（teardown_flows / hook_controller_teardown / nested_modal_grab） | すべて pass | すべて pass |
| `user/` `quarantine/` の生成 | なし | なし |

**順序依存なし**（3 モジュールとも単独実行で pass）。

## 2. 二次レビュー

- **`deep-reviewer`（フェーズ横断）= 完了可**（task_01〜03 の範囲）。修正必須の欠陥なし。
  5 観点すべて OK。phase.md の固有観点 7 項目に構造的な根拠とテストが揃っていることを確認。
  - **順序の担保**: `grab_modal` の復元（同期）→ フック解除（`after(0)`）の順は
    **バインド順ではなく遅延実行が担っている**ため、将来 `grab_modal` の位置が動いても壊れない。
  - **終了ガードの位置**: `app.py:530` は `confirm_save_if_dirty` の後・`stop_hook()` と
    `self.destroy()` の前。**キャンセル時には立たない**。呼び出し元は 1 箇所のみ。
- **`codex-reviewer`（標準レビュー）= 指摘なし**。
  - **制約**: `review` コマンドは focus text を受け付けないため、依頼した観点は渡っていない
    （渡ったのは `--base ecdb3eb` のみ）。**観点指定のレビューはフェーズ完了判定前の
    `codex-adversarial-reviewer` で実施する**（`.claude/rules/agent_selection.md` の表）。

## 3. 指摘の処理（ユーザー判定 2026-09-13）

| # | 指摘 | 判定 | 対応 |
|---|---|---|---|
| F1 | `tests_ui` 全体実行時の stderr `invalid command name` | **修正して採用（テスト側のみ）/ production は除外** | App 破棄前に `update()` を 1 回入れる規約を **13 箇所**へ適用。**フック解除由来は 6 件中 2 件で、これは 0 件になった**。残る 4 件は**ステータスバーの 4 秒タイマー**（`_clear_flash_message`・phase 15 以前からの既存事象で、ドレインでは流せない）。**記録のみ**とする |
| F3 | 受け入れ条件 7 の担保が `scan_dirs` に偏る | **採用** | 静的検査 3 を追加（T2 が残る 4 クラスに `destroy` override が存在する）。変異検査で fail を確認 |
| F4 | `window` の型注釈が無い | **採用** | `window: tk.Misc | None = None` + `import tkinter as tk` |
| F9 | 停止カウンタが**入力の扱い**にも使われる点が仕様に無い | **採用（task_05 で反映）** | 正本 `key_input.md` §7.2 へ 1 句加える。**当初案の「入力を通さない」は誤り**（完了判定前の敵対的レビューが検出）。実際は **`accept=True` で元入力を素通し**し、**置換とアクション実行だけを行わない**。この趣旨で昇格した |
| F5 | テストが private 属性 `_shutting_down` を patch | **保留（受容）** | 終了確定は不可逆であるべきで `end_shutdown` は足さない。属性名変更時にテストが壊れることを許容 |
| F6 | 静的検査が発見ベースでない（新規ダイアログを守らない） | **除外** | phase 14 からの既存保留項目へ合流（`current.md`） |
| F11 | 終了中の `start_hook` 早期 return がボタン同期をスキップ | **除外** | 終了中にウィジェットへ触らないのが正。問題なし |

**残存リスク（記録のみ・受容）**: `confirm_save_if_dirty("終了")` は終了ガードより前に走るため、
その中の子ファイル保存ダイアログの `finally` 同期解除で `start_hook()` が動き得る。
キャンセル可能な区間なのでこの順序が正しく、暫定仕様 §4-3 の指定どおり。発生条件は極小。

### 是正 3 件の実測

- `tests_ui` **321 pass**（静的検査 1 本増）。`keyseq/` の差分は `hook_controller.py` の型注釈のみ。
- **変異検査**: `trigger_dialog.py` の `destroy` を改名すると静的検査 3 が fail（復元済み）。

## 4. 実機目視（ユーザー実施）

（結果を記入する。4 項目 = ① × 閉じ後に次のダイアログでフックが止まる
② OK / キャンセルでも同様 ③ネストしたダイアログの × 閉じで親のモーダル性が戻る
④フック ON のまま × 終了してエラーダイアログが出ない）

- 状態: **実施済・全 4 項目で問題なし**（ユーザー 2026-09-13）。
- **対象ダイアログ = 「ホットキープリセット編集」**（`protocol` 未登録の 5 クラスの 1 つ。
  × 閉じで後始末を取りこぼしていた側の代表）。
- **判定方法**: フック ON でボタン表示が「停止（フックOFF）」→ ダイアログを開くと
  「開始（フックON）」へ戻り、閉じると再び「停止（フックOFF）」へ戻ることを見る
  （`stop_hook` / `start_hook` が `sync_hook_toggle_buttons` を呼ぶため）。
- **他クラスでの追試は不要と判断**（ユーザー確認 2026-09-13）。根拠 = ①静的検査が 8 クラスの
  呼び出し形を固定している ②自動テストが代表 2 クラス（× 閉じ）+ 1 クラス（Escape）で実経路を通す
  ③暫定仕様 §5 が「× 閉じは代表 1〜2 クラスで見る。全 8 クラスは静的検査が守る」と規定している。
