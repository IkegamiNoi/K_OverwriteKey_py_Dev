# task_05_integration_and_manual_check

## 目的

phase 14 の task_01〜04 を通した**統合確認**と**二次レビュー**を行い、
暫定仕様 [12](../../../history/12_nested_modal_grab_restore.md)（v0.5）§7 の受け入れ条件のうち
**自動テストで見られる 1〜12** と **実機目視でしか見られない 14** を満たす。

**コードは変更しない**（統合確認で不具合が出た場合のみ、最小修正を別途判断する）。
**レイヤ制約**: presentation 限定・スキーマ不変（task_01〜04 と同じ）。

## 対象範囲（確認とレビューのみ・実装なし）

### 1. 統合確認（`verifier` へ委任）

phase 全体の差分（`6f6e300..HEAD`）に対して実行する。

- `python -m compileall -q keyseq main.py tests tests_ui` が clean。
- `python -m unittest discover -s tests` = **pass 417 / skip 7**。
- `python -m unittest discover -s tests_ui` = **pass 303**。
- `python -m tests.smoke_app` が pass。
- **受け入れ条件 10 の再確認**: `grep -rn "grab_set" keyseq/presentation/ --include=*.py` が
  **`modal.py` の 2 行のみ**、`transient` が **`modal.py` の 1 行のみ**。
- テスト実行後に worktree ルートへ `user/` / `quarantine/` が生成されていないこと。

### 2. 二次レビュー（複数タスクを跨ぐ差分）

`.claude/rules/agent_selection.md` のレビュー表「統合テスト・複数タスクを跨ぐ動作確認時」に従い、
**`deep-reviewer`（Claude 側）と `codex-reviewer`（Codex 側）を併用**する。
対象は **`6f6e300..HEAD` の差分全体**（`keyseq/` + `tests_ui/` + 暫定仕様 v0.5 の改訂）。

観点:

- **受け入れ条件 1〜12 が実際に満たされているか**（条件ごとにどのテストが対応するかの対応付け）。
- **v0.5 の §3-6 改訂が仕様・タスク定義・実装・テストで一貫しているか**。
- **task をまたいで生じた齟齬**（task_01 のヘルパ設計と task_02〜04 の使い方のズレ）。
- **テストが偽 pass になっていないか**（task_03 / task_04 で変異検査済だが、別視点で再確認）。
- スコープ逸脱（スケルトン共通化・親付け替え・stdlib 対応を取り込んでいないか）。

**指摘は提示のみ**。修正の採否はユーザー判断（Codex レビューの扱いは `agent_selection.md`）。

### 3. 実機目視（**ユーザーが実施**）

暫定仕様 §7-14 と §6-3 が対象。**下表の結果をユーザーから受け取るまで本タスクを完了にしない**。

#### A. ネスト経路（自動テスト済みだが実機でも確認する）

| # | 操作 | 期待 |
|---|---|---|
| A1 | プリセット編集を開く → 「追加」→ 追加ダイアログを **OK で閉じる** | プリセット編集がモーダルのまま（背後のメインウィンドウをクリックしても反応しない） |
| A2 | 同上 → 追加ダイアログを **× で閉じる** | 同上 |
| A3 | アクション編集を開く → プリセット編集を開く → プリセット編集を閉じる | アクション編集がモーダルのまま |
| A4 | 個別プリセットで保存先に別内容のファイルがある状態でプリセット編集の OK → **上書き確認を「キャンセル」** | **プリセット編集が開いたまま**でモーダル（= 本フェーズの本命） |

#### B. stdlib ダイアログ（§6-3・**未検証扱いの 4 経路**）

いずれも「stdlib のダイアログを閉じた後、**それを開いた親ダイアログがモーダルのままか**」を見る。

| # | 場所 | 操作 |
|---|---|---|
| B1 | 孤児ファイルの棚卸しダイアログ（`orphan_sweep_dialog.py:52`） | 走査ディレクトリの追加でフォルダ選択を開き、**選択とキャンセルの両方**で閉じる |
| B2 | 子ファイル保存の一覧ダイアログ（`child_save_dialog.py:213`） | 子に「別名保存」を選んで実行し、ファイル保存ダイアログを**保存とキャンセルの両方**で閉じる |
| B3 | 依存確認ダイアログ（`child_save_dialog.py:264`） | trigger_set の別名保存でファイル保存ダイアログを開いて閉じる |
| B4 | 再計算後の上書き確認（`child_save_dialog.py:303`・`:314`） | メッセージボックスと、続くファイル保存ダイアログを閉じる |

**期待**: 親がモーダルのまま（背後を触れない）。**Windows ではネイティブのコモンダイアログに
マップされ Tk の grab を触らないため、問題なしが想定**（暫定仕様 §6-3）。
**問題が出た場合はフェーズのスコープを広げるかユーザー判断**（現時点では対象外と確定済み）。

#### C. 退行確認（grab 以外を壊していないこと）

| # | 操作 | 期待 |
|---|---|---|
| C1 | 各ダイアログを開いて Escape / × / キャンセルで閉じる | 従来どおり閉じる。表示位置・前面表示が変わっていない |
| C2 | ダイアログを開いている間のフック状態 | 従来どおり停止し、閉じたら再開する（**× 閉じの既存不具合は [idea_16] で別管理**） |

## 含まない

- **正本反映・暫定仕様 12 の凍結・`ActionDialog` 親付け替えの idea 起票・`/refactor_check`**
  → **task_06**（受け入れ条件 13 / 15 も task_06）。
- **× 閉じでフック再開が走らない不具合の是正** →
  [idea_16](../../../backlog/idea_16_wm_close_skips_destroy_override.md)（フェーズ外）。
- **stdlib ダイアログの grab 対応**（B で問題が出た場合も、是正は別途ユーザー判断）。
- コード変更全般（統合確認で不具合が出た場合は**報告して停止**し、修正は枝番タスクで行う）。

## 確認

1. 「対象範囲 1」の統合確認がすべて pass（`verifier` の報告で確認）。
2. 「対象範囲 2」の二次レビュー 2 本が完了し、**ブロッキング指摘が無い**
   （指摘があればユーザー判断を経て、必要なら枝番タスク `task_05b_*` を起票）。
3. 「対象範囲 3」の実機目視 **A1〜A4 / B1〜B4 / C1〜C2** の結果をユーザーから受け取り、
   **期待どおりでない項目が無い**（または、あった項目の扱いをユーザーが判断済み）。
4. 受け入れ条件 1〜12 と 14 の充足状況を一覧で完了報告に含める。

## 完了条件

- 上記「確認」1〜4 をすべて満たす。
- **`deep-reviewer` + `codex-reviewer` の二次レビュー実施**（本タスクではこれが必須レビューを兼ねる。
  `agent_selection.md` の「統合テスト・複数タスクを跨ぐ動作確認時」）。
- **実機目視は本タスクで実施**（ユーザーが実行し、結果を報告する）。
