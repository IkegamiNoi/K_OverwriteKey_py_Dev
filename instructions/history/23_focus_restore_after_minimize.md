# 暫定仕様 23: 最小化から復元した後のキーボードフォーカス（focus_restore_after_minimize）

> 状態: **未凍結・v0.5・主入力（ユーザー確定済 2026-09-23・実装着手可）**。本書がこのフェーズの確定設計（フェーズ中は正本を直接改訂しない）。
> フェーズ末タスクで正本 `instructions/common/spec_detail/` へ昇格し本書を凍結する。
> 起票元: [idea_34](../backlog/idea_34_focus_restore_after_minimize.md)（phase 28 task_05 の実機目視 C② で再現・2026-09-23）。
> 番号対応: phase 29 / 暫定 23 / decisions 29。
> **v0.2 = 起票時レビュー（`deep-reviewer`・修正要）+ メインの追加実測を反映**。時点の根拠をキュー経由の復元で再測定し
> 代替案（`<FocusIn>` 方式）を反証（§1.2）/ 戻し先の定義を 1 つに統一（§3.1-2）/ 既存条項との関係を API 単位で書く案へ（§4-1）/
> phase 17 の禁止理由の要約を訂正（§1.1）/ テスト経路（a6・a9・a10 相当）と実機目視（3 段・他アプリ・Win+D 2 回目・Alt+Tab で最小化窓を選ぶ）を追加。
> **v0.3 = 確定前の敵対的レビュー（`codex-adversarial-reviewer`・needs-attention 2 件）を反映**。①最小化中に開いたモーダルは
> 初期フォーカス要求（phase 28 の正本保証）を優先し、復元時のフォーカス復帰を行わない（§3.1-6・§5 ⑧）②「最後のフォーカス先」へ戻すことを
> 初期フォーカス先と別の widget で検査する（§5 ⑨・§6-6⑦）。
> **v0.4 = task_01 の実測で §3.1-4 を訂正（ユーザー確定 2026-09-23）**。実 App の 3 段ネストで `<Map>` 内の即時 `focus_set` が
> **一番外側のダイアログを非表示のまま残す**退行を起こした（既存テスト a2・a3 が検出・§1.2 測定 3）。→ **フォーカス復帰だけ `after_idle` で予約**する
> （grab の返却は `<Map>` 内で即時のまま）。あわせて §3.1-6 の記録の寿命を**その回の復元まで**に限定（`reviewer` の再検討推奨）。
> **v0.5 = task_02 の実機目視①が不合格（Win+D → タスクバー復元で Escape が届かない）→ §3.1-3 を訂正**。復元時に OS がメイン窓を
> アクティブにする時点では grab が預かり中で Tk の振り向けが働かず、Tk がフォーカスを持たないため `focus_set` が効かない（§1.2 測定 4）。
> → **「Tk がフォーカスを持たず、かつ OS の前面が自アプリのメイン窓」のときだけ `focus_force`**（§3.1-3・§3.2・§5 ⑩〜⑭・§6-4・§6-6⑤）。
> 確定前の敵対的レビュー（`codex-adversarial-reviewer` needs-attention）で非奪取の保証範囲（TOCTOU 受容・§7）と FFI の型契約（§3.2）を追記。

---

## §1 目的 / 背景

モーダルダイアログを開いたままアプリを最小化し、復元すると、**モーダル性（grab）はダイアログへ戻るが
キーボードフォーカスはメイン窓に残る**。クリックしない限り **Escape などのキーがダイアログへ届かない**
（Escape はメイン窓へ行く）。**復元時にフォーカスも最内の表示中モーダルへ戻す**。

**対象レイヤ = presentation のみ**（`keyseq/presentation/modal.py`）。スキーマ・domain / application の変更なし。

### §1.1 現状監査（2026-09-23・メイン実測・起票時レビューで裏取り済）

- `modal.py:33-59` `return_custody`（`install_minimize_grab_custody` 内・App の `<Map>`）は、預かった窓
  （無ければ台帳 `_active_modals` の最内の「生存かつ表示中」の窓）へ **`grab_set()` だけ**を行い、フォーカスには触れない。
  **預かりが空なら何もしない**（`:38-39`）。**別の窓が既に grab 中なら何もしない**（`:46-47`）。
  ガード `event.widget is not app`（`:35`）で `pack_forget` 等に由来する子の `<Map>` は除外済み。
- 正本 `features.md` §4.6「モーダルダイアログの作法」:
  - 最小化の条項 = 「**復元後もモーダル性は戻る**」「**復元の際、アプリは窓の表示・前面を操作しない**
    （OS が戻した状態のまま、モーダル性だけを戻す）」（`:187-208` 付近）。
  - **phase 17 の禁止理由の正確な内容**（暫定仕様 15 §1.2 / §3）: **`deiconify` は中間窓が消えると実測**。
    **`lift` / `focus_force` は「WM が既に戻しているので不要」**として呼ばない。
    「他アプリから入力を奪わない」は **phase 28**（暫定仕様 22 §3.1-4）の理由で、初期フォーカスの条項（`:174-178`）に属する。
  - phase 28 で追加 = 「**閉じた後・最小化から復元した後のキーボードフォーカスの戻り先は規定しない**」（本フェーズで改める対象）。
  - 保証範囲外（`:218-219`）= 最小化中にアプリ側が新しいモーダルを開いた場合。
- 固定テスト `tests_ui/test_minimize_grab_custody.py`（321 行・13 件）。**a4（`:145-163`）が復元時の app・dialog の
  `deiconify` / `lift` / `focus_force` を 0 回に固定**（`focus_set` は固定対象外）。a6（別の窓が grab 中）/ a9・a10（最小化中に
  新しいモーダルを開閉）の組み立てがある（`:178-273`）。
- `install_minimize_grab_custody` の呼び出しは `app.py:206` の 1 箇所。
- `codebase_map.md:312-313` は「推測型（`focus_lastfor()`）は不採用」と書くが、これは**初期フォーカス**の判断
  （未マップ時は保留され読めない）。本件は**表示済みの窓の記録を読む**ので別物（§8 で範囲を限定する改訂を入れる）。

### §1.2 実測（2026-09-23・`.venv` python で probe）

**測定 1**（API による模擬: `iconify` → `ShowWindow(SW_RESTORE)` + `SetForegroundWindow` を同一スレッドで → 実 Escape を `keybd_event`）:

| 条件 | 復元直後の `focus_get` | 前面の窓 | 実 Escape の行き先 |
|---|---|---|---|
| 何もしない（現状） | **`.`（メイン窓）** | メイン窓 | **メイン窓**（C② を再現） |
| `<Map>` 内で即 `focus_set` | 一巡後 **ダイアログの Entry** | **ダイアログ** | **ダイアログ** |
| `<Map>` から `after_idle` で `focus_set` | 同上 | ダイアログ | ダイアログ |

**測定 2**（キュー経由の復元: `iconify` → `PostMessageW(WM_SYSCOMMAND, SC_RESTORE)`。**3 段ネスト**〔root → d0 → d1 → d2・transient 連鎖〕）:

| 条件 | App のイベント順 | 復元後の `focus_get` | 3 窓の表示 | 実 Escape の行き先 |
|---|---|---|---|---|
| 何もしない | **`<FocusIn>` → `<Map>`** | `.` | 3 窓とも表示 | **メイン窓** |
| `<Map>` 内で即 `focus_set` | 同上 | **最内の Entry** | **3 窓とも表示** | **最内** |
| `<Map>` で印 → 次の `<FocusIn>` で戻す（レビューの代替案） | 同上 | `.` | 3 窓とも表示 | **メイン窓（戻らない）** |

- **キュー経由の復元では App の `<FocusIn>` が `<Map>` より先に届く**。そのため `<FocusIn>` を待つ方式は**戻らない**
  （反証）。`<Map>` 内で即時に `focus_set` する方式は両測定で成立し 3 段ネストでも中間窓は消えなかったが、**実 App では外側の窓が戻らない退行が出た**（測定 3。v0.4 で `after_idle` へ訂正）。
- **`<Map>` の時点で `focus_lastfor()` は最小化前のフォーカス先を返す**（表示済みの窓の記録。phase 28 の「未マップ時は保留」とは別）。
- **`focus_set` が別の Toplevel をアクティブにするのは、アプリが OS の入力フォーカスを持つときだけ**。持たないとき
  （測定 2 の 1 段構成で、プロセスが前面を取れなかった回）は `focus_get` が `None` のままで、何も奪わない
  （phase 28 の機序 3 と整合）。→ **他アプリで作業中のユーザーから入力を奪わない**のは Tk / OS の側で保たれる。
- ※ いずれも API による模擬。**タスクバー・Win+D・Alt+Tab での実際の復元は実機目視で確定する**（§6-6）。

**測定 3**（v0.4・task_01 の実装で既存テスト a2 / a3 が落ちたため実 App で再測定。アクション編集 → プリセットマネージャ →
上書き確認〔`confirm_overwrite`〕の 3 段・`app.iconify()` → `app.deiconify()`）:

| 条件 | 復元後の表示（アクション / マネージャ / 確認） |
|---|---|
| フォーカス復帰なし | **1 / 1 / 1** |
| `<Map>` 内で即時 `focus_set` | **0 / 1 / 1**（**アクション編集が非表示のまま**。1.3 秒後も戻らない） |
| `after_idle` / `after(0)` / `after(50)` / `after(200)` で `focus_set` | **1 / 1 / 1**・フォーカスは最内（確認ダイアログ） |

- **実 App では、Windows が所有窓を順に戻している途中で `focus_set` が割り込むと外側の窓が戻らない**（素の Tk の probe では
  再現しなかった）。**`after_idle` へ後ろ倒しすると解消**する。測定 1 でも `after_idle` はダイアログへ Escape を届けていた。
- `<FocusIn>` を待つ方式（測定 2 で反証）とは別物。`after_idle` は `<Map>` の処理が一巡した直後に走る。

**測定 4**（v0.5・task_02 の実機目視①が不合格。実 App + 実操作〔Win+D → タスクバーから復元〕をイベントログ付き probe で記録。
アクション編集を開いた 1 段）:

| 時点 | OS の前面窓 | Tk の `focus_get()` | 出来事 |
|---|---|---|---|
| 復元（App `<Map>`） | **メイン窓** | **`None`** | App の `<FocusIn>` は届かない |
| 予約した復帰処理（v0.4 実装） | メイン窓 | `None` のまま | 戻し先 = アクション編集の値欄（§3.1-2 は正しく解決）。**`focus_set` の後も `None`** |
| 以後 2 秒 | メイン窓 | `None` | **Escape は Tk に届かない**（ログに無い）。タイトルバーはメイン窓がアクティブ色 |
| 他アプリへ移って戻る | **アクション編集** | 値欄 | grab 保持中のアクティブ化は Tk が grab 保持者へ振り向ける → Escape で閉じる |

- **復元時に OS がメイン窓をアクティブにする時点では grab が預かり中**（phase 17 の預かり）で、Tk の振り向けが起きない。
  Tk はフォーカスを持たないため `focus_set` は記録に留まる（§1.2 末尾の「アプリが OS の入力フォーカスを持つときだけ」の実例）。
  測定 1〜3 は API による復元で、Tk がフォーカスを持つ経路だったため再現しなかった。
- **候補 A（§3.1-3 の条件つき `focus_force`）を probe で実機確認**: ①（Win+D → タスクバー復元）で 2 回とも発動し、フォーカスがダイアログへ移り
  Escape で閉じた。②も OK（ユーザー報告。ただしログ上、最小化時点の grab 保持者はプリセットマネージャ＝2 段で、3 段目が開いていたかは未確認）。
  ⑤（メモ帳が前面）の 4 回の復元では**すべて条件 (b) で不発**・Tk の `focus_get()` は `None` のまま（keyseq は何も取っていない）。
- ⑤でメモ帳のカーソルが出ない・文字が打てないのは **keyseq を閉じてメモ帳単独で Win+D 2 回でも起きる**（ユーザー確認）＝ Windows 側の挙動。

---

## §2 確定事項（ユーザー 2026-09-23）

- **v0.5 をユーザー確定**（2026-09-23・`codex-adversarial-reviewer` needs-attention 2 件を推奨どおり反映: 非奪取は判定時点の最善努力で
  TOCTOU を受容〔§3.1-3・§7〕/ FFI の型契約〔§3.2・§5 ⑭〕。`deep-reviewer` M1 = 既知の限界として記録〔§7〕/ M2 = §3.1-6 も昇格〔§8〕）。
  経緯: task_02 の実機目視①が不合格 →
  候補 A（条件つき `focus_force`・§3.1-3）を probe で実機確認（①② OK・⑤で発動せず）→ 採用方針。
  ⑤でメモ帳のカーソルが出ないのは **keyseq を閉じても起きる Windows の挙動**（ユーザー確認）で、判定対象外（§6-6⑤）。
- **idea_34 を phase 29 として扱う**（暫定仕様先行モード）。
- **最小化から復元した後、キーボードフォーカスを最内の表示中モーダルへ戻す**（C② の解消）。
- **v0.4 をユーザー確定**（2026-09-23）: フォーカス復帰は `after_idle` で予約する（§3.1-4）/ 最小化中に開いた窓の記録はその回の復元まで（§3.1-6）。
- **v0.3 をユーザー確定**（§4 の 3 点は提案どおり）:
  1. 既存条項「復元の際、窓の表示・前面を操作しない」を **API 単位**へ言い換える = 「`deiconify` / `lift` / `focus_force` を呼ばない。
     キーボードフォーカスは `focus_set` で最内の表示中モーダルへ戻し、それに伴うアクティブな窓・重なり順の変化は許容する」。
     初期フォーカスの条項（「窓の前面化・フォーカス強制は行わない」）も同じ言い回しに揃える。
  2. **閉じた後のフォーカスは規定しない**（現状維持）。正本の一文は「最小化から復元した後は戻す／閉じた後は規定しない」に分ける。
  3. **最小化を伴わない再アクティブ化は対象外**（実機目視で挙動を記録するのみ）。**Alt+Tab で最小化中の窓を選ぶ復元は対象内**。
  4. **最小化中に開いたモーダルには復帰を行わない**（§3.1-6）/ 「最後のフォーカス先」を初期フォーカス先と別の widget で検査する（§5 ⑨）。

---

## §3 設計

### §3.1 規範（実装が従う条項）

1. **アプリを最小化から復元したとき、モーダルダイアログが開いていれば、キーボードフォーカスをそのモーダルへ戻す**。
   戻し先の widget は**そのモーダルの中で最後にフォーカスがあった widget**（`focus_lastfor()`。記録が無い・`None` なら
   モーダル自身）。
2. **戻し先の窓の定義（1 つ）**: **復元処理（grab の返却）を終えた時点の grab 保持者**（`app.grab_current()`）が
   **台帳 `_active_modals` に同一性（`is`）で含まれ、かつ表示中**なら、その窓。
   - 預かりあり → 返却した窓 / 預かりなし → もともとの保持者 / 別の窓が既に grab 中 → その窓（台帳にあれば）、を
     **この 1 つの定義で覆う**（経路で分けない）。
   - **grab 保持者が解決できない（stdlib ダイアログ）/ いない / 台帳に無い / 表示されていない場合は触らない**。
3. **既定は `focus_set`**。`lift` / `deiconify` は呼ばない（既存テスト a4 の固定を維持）。
   **`focus_force` は次の 2 条件を両方満たすときだけ、同じ戻し先へ使う**（v0.5・§1.2 測定 4）:
   (a) `focus_set` の後も **Tk がフォーカスを持たない**（`app.focus_get() is None`）、かつ
   (b) **OS の前面窓が App のメイン窓**（`GetForegroundWindow()` == `int(app.wm_frame(), 16)`）。
   - 理由: 復元時に OS がメイン窓をアクティブにする時点では grab が預かり中（誰も保持していない）で、Tk 自身の
     「grab 中にアクティブ化された窓 → grab 保持者へ振り向ける」処理が働かない。その結果アプリは前面なのに Tk はフォーカスを
     持たず、`focus_set` は「次に来たときの行き先」の記録に留まりキーが届かない。`focus_force` はこの欠けた振り向けを補う。
   - (b) により**他アプリが前面のとき（§6-6⑤）は何もしない**（入力を奪わない）。(a) により Tk が既にフォーカスを持つ経路
     （API による復元・テスト環境の多く）では従来どおり `focus_set` だけで終わる。
   - 前面窓の取得に失敗した場合（非 Windows・`ctypes` の例外・NULL）は (b) を偽として扱う（`focus_set` のみ）。
   - **「他アプリから入力を奪わない」は判定時点での最善努力**（v0.5・敵対的レビュー high）: (b) の判定と `focus_force` は同じ処理の中で
     連続して行うが、その間に OS の前面が変わる競合（TOCTOU）は排除できない。残存リスクとして受容する（§7）。
4. **App 自身の `<Map>` で grab を返却した後、フォーカス復帰は `after_idle` で予約する**（v0.4。grab の返却は `<Map>` 内で即時のまま。
   既存ガード `event.widget is app` を流用）。**戻し先（§3.1-2）は予約が実行された時点で決める**（その間に窓が閉じられても安全）。
   **即時に `focus_set` してはならない**（§1.2 測定 3: 実 App の 3 段ネストで外側の窓が戻らない）。
5. **失敗は `TclError` / `KeyError` を握る**（破棄中の窓、`focus_lastfor()` の名前解決失敗）。復元・終了を妨げない。
6. **最小化中に開いたモーダルには復帰を行わない**（v0.3）。その窓の初期フォーカス要求は未マップのため Tk 内で保留中で
   `focus_lastfor()` から読めず、`focus_set(窓自身)` を出すと**開いたときの明示指定を奪う**（正本の初期フォーカス条項 `features.md:174-178` に反する）。
   `grab_modal` が `_app_minimized` の間に呼ばれた窓を記録し（台帳と同じモジュール状態。破棄時に除く）、
   §3.1-2 の窓がそれに当たる場合は `focus_set` しない（保留中の要求が復元時に解決される）。
   **記録の寿命はその回の復元まで**（v0.4）: 予約したフォーカス復帰の処理を終えた時点で記録を空にする。以後の最小化 → 復元では
   その窓も通常どおり戻す（初期フォーカス要求はその回の復元で解決済みのため）。

### §3.2 実装方式

- `modal.py` にフォーカス復帰の関数を切り出す（例: `_restore_modal_focus(app)`）。§3.1-2 の定義で窓を決め、
  `target = window.focus_lastfor() or window` へ `focus_set()`。台帳の判定は `restore_grab` と同じく **`is` による同一性**。
- `return_custody` は**どの経路でも最後にフォーカス復帰を `after_idle` で予約する**（v0.4。預かりが空の早期 return もこの予約を通る形へ
  整理する）。`return_custody` は現在 27 行のため、**戻し先の決定は別関数に出して 30 行目安を守る**。
- `focus_lastfor()` は Tk の仕様上、**同じ Toplevel 内の widget か、その Toplevel 自身**を返す（別の Toplevel は返さない）。
- （v0.5）§3.1-3 の (b) は `modal.py` 内の非公開関数（例: `_is_app_foreground(app) -> bool`）に閉じる。`ctypes.windll.user32.GetForegroundWindow`
  を使い、例外（`AttributeError` / `OSError` / `TclError` / `ValueError`）は `False` を返す。**presentation で `ctypes` を使うのはこの 1 箇所**
  （OS の窓状態の問い合わせで、UI 層の責務。infrastructure の `input_gateway` へは寄せない）。テストはこの関数を `patch.object` で差し替える。
  判定と `focus_force` は §3.1-4 の予約した処理の中で `focus_set` の直後に行う（別の予約を増やさない）。
- （v0.5・敵対的レビュー medium）**FFI の契約**: 共有の `ctypes.windll.user32` の属性は変えず、**専用の `ctypes.WinDLL("user32")`** から
  `GetForegroundWindow` を取り、**`argtypes = []` / `restype = ctypes.wintypes.HWND`** を設定する（既定の符号付き `int` だと HWND 値によって
  符号・桁が崩れ、`wm_frame()` と一致せず無言で失敗する）。戻り値が `None`（NULL）なら偽。比較は `int(app.wm_frame(), 16)` との整数比較。

### §3.3 閉じた後のフォーカス（参考・変更しない）

ネストの内側を閉じた後は、Tk の既定の動作で外側へフォーカスが戻る（phase 28 の probe 3 の H・実機目視 C① で確認済）。
**本フェーズでは規定を追加しない**（§4-2）。
phase 28 の「フォーカスを `grab_modal` の外で設定しない」（22 §3.1-3）は**初期フォーカス**の条項であり、本件の復元時の
フォーカス復帰とは範囲が異なる（衝突しない）。

---

## §4 確認事項（→ 2026-09-23 ユーザー確定。§2 へ移した・経緯として残す）

1. **既存条項「復元の際、アプリは窓の表示・前面を操作しない」との関係**。`focus_set` で**アクティブな窓がダイアログに変わる**
   （§1.2）。**これは「前面（アクティブな窓）を操作しない」を意図的に緩める判断**になる。
   - **提案（API 単位で書く）**: 「復元の際、アプリは **`deiconify` / `lift` / `focus_force` を呼ばない**（OS が戻した表示状態のまま）。
     **キーボードフォーカスは `focus_set` で最内の表示中モーダルへ戻す**（それに伴うアクティブな窓・重なり順の変化は許容する）」。
     初期フォーカスの条項（「窓の前面化・フォーカス強制は行わない」）も同じ言い回しに揃える。
   - 理由: 「重なり順は変えない」と書くと、`transient` を失った窓（呼び出し元を先に閉じた子。`features.md:200-202`）では
     アクティブ化で重なり順が変わりうるため偽になりうる（起票時レビュー H2・未実測）。**呼ぶ API で書けばテスト a4 の固定と一致する**。
2. **「閉じた後」のフォーカスも規定するか**。
   - **提案: 規定しない（現状維持）**。Tk の既定で外側へ戻っており（C① OK）、実装を足す理由が無い。
     正本の一文は「**最小化から復元した後**は戻す／**閉じた後**は規定しない」に分ける。
3. **最小化を伴わないアプリの再アクティブ化**（Alt+Tab で最小化されていない窓へ戻る・クリック）も対象にするか。
   - **提案: 対象外**。App の `<Map>` が来ないため別の結線が要り、範囲が広がる。実機目視で挙動だけ記録し、
     フォーカスが外れる事象があれば idea 起票。
   - ※ **Alt+Tab で最小化中の窓を選んで復元する**のは `<Map>` が来る経路なので**本フェーズの対象内**（§6-6）。

---

## §5 テスト

- **決定論的な単体検査**（`tests_ui/test_minimize_grab_custody.py` に追加）。既存の最小化 → 復元の組み立てで、
  **戻し先の widget の `focus_set` が 1 回呼ばれる**ことを `patch.object(..., wraps=...)` で固定する。
  **期待値はダイアログごとの具体 widget で固定**する（例: `ActionDialog` なら phase 28 の初期フォーカス先。
  `focus_lastfor()` の戻り値をそのまま期待値にしない＝実装をなぞるだけの検査にしない）。経路:
  ①預かりあり ②預かりなし ③別の窓が既に grab 中（a6 相当）④最小化中に新しいモーダルを開いて閉じた（a9 相当 → 元の窓へ）
  ⑤預かった窓と新しいモーダルを両方閉じた（a10 相当 → 外側へ）⑥grab 保持者が解決不能（stdlib 相当）→ `focus_set` 0 回
  ⑦台帳に無い窓 → 0 回 ⑧**最小化中に `grab_modal` した窓**（`_app_minimized` を立てた状態で開く）→ 復元時の `focus_set` 0 回・
  開いたときに指定した `focus=` の widget への要求が残る ⑨**最小化前に初期フォーカス先とは別の widget へフォーカスを移してから**
  最小化 → 復元 → **その別の widget へ** `focus_set`（例: `ActionDialog` で `value_entry` ではなくラベル欄。毎回初期フォーカス先へ戻す
  誤実装を落とす）。
- **a4（`deiconify` / `lift` / `focus_force` が 0 回）は `_is_app_foreground` が `False` の条件で維持する**（v0.5。
  `deiconify` / `lift` はどの条件でも 0 回）。
- （v0.5）**条件つき `focus_force`（§3.1-3）の検査**。`_is_app_foreground` と `app.focus_get` を `patch.object` で差し替え、
  期待値は具体 widget（`ActionDialog` なら `value_entry`）で固定する:
  ⑩前面 = 真・`focus_get()` = `None` → 戻し先へ **`focus_force` 1 回**（`focus_set` も 1 回・`lift` / `deiconify` 0 回）
  ⑪前面 = 真・`focus_get()` が widget を返す → `focus_force` 0 回 ⑫前面 = 偽（他アプリ前面 = §6-6⑤相当）・`focus_get()` = `None` →
  `focus_force` 0 回 ⑬§3.1-2 で触らない窓（台帳外・最小化中に開いた窓）では前面 = 真・`None` でも `focus_force` 0 回
  ⑭`_is_app_foreground` 自体: `GetForegroundWindow` の戻り値が `wm_frame()` と一致 → 真 / 不一致 → 偽 / 例外 → 偽
  （`ctypes` 側を差し替えて検査。実 OS の前面状態に依存させない）/ **最上位ビットが立つ HWND 値**（例 `0x8000_1234`）でも一致判定が真 /
  戻り値 `None`（NULL）→ 偽 / 取得に使う関数に **`restype` が `wintypes.HWND`・`argtypes` が空**で設定されている（非 Windows では skip）。
- （v0.5）**変異検査**: `focus_force` の分岐を外す → ⑩赤 / 条件 (b) を外す → ⑫赤 / 条件 (a) を外す → ⑪赤。
- **ネスト 3 段**（既存 a2 の組み立て）で、復元後に**3 段とも表示されたまま**・**最内へ `focus_set`** が出ること。
  **注記**: テスト環境ではアプリが OS フォーカスを持たないことが多く、その場合 `focus_set` は記録だけでアクティブ化の経路を
  通らない。**中間窓が消えないことの最終確認は実機目視（§6-6②）で行う**。
- **実 Tk の到達検査**（`focus_get()` 依存）を置く場合は phase 28 の skip ガード方式にし、**修正前に落ちることを確認する**
  （落ちなければ参考扱いとし、受け入れの根拠にしない）。skip 件数は完了報告に記す。

---

## §6 受け入れ条件（ドラフト）

1. 最小化 → 復元の後、**§3.1-2 の窓の最後のフォーカス先**へ `focus_set` される。**初期フォーカス先と別の widget にいた場合もその widget へ戻る**（§5 ①②⑨）。
2. 別の窓が grab 中 / 最小化中の開閉（a6・a9・a10 相当）でも §3.1-2 の定義どおりに戻す（§5 ③④⑤）。**最小化中に開いた窓には復帰を行わず、開いたときの初期フォーカス要求が残る**（§3.1-6・§5 ⑧）。
3. **stdlib ダイアログ・台帳外・非表示では触らない**（§5 ⑥⑦）。
4. **`lift` / `deiconify` は 0 回のまま**（既存 a4 が green）。**`focus_force` は §3.1-3 の 2 条件を満たすときだけ 1 回**（v0.5・§5 ⑩〜⑭）。
5. 既存の `tests_ui/test_minimize_grab_custody.py` 13 件・`tests` / `tests_ui` 全体・smoke が green。
6. **実機目視**（ユーザー）:
   ①ダイアログ（例: アクション編集）を開いたまま **Win+D → タスクバーから復元** → **クリックせずに Escape で閉じる**
   ⑦アクション編集で**初期フォーカス先ではない入力欄（ラベル等）をクリックして入力中**に Win+D → 復元 →
     **クリックせずに文字を打つと、その入力欄へ入る**（初期フォーカス先へ戻っていない）
   ②**3 段ネスト**（アクション編集 → プリセットマネージャ → 追加・編集）で Win+D → 復元 → **3 窓とも表示されたまま**・
     Escape で最内から 1 つずつ閉じる
   ③**Win+D をもう 1 回押して復元**した場合も①が成り立つ
   ④**Alt+Tab で最小化中のアプリを選んで復元**した場合も①が成り立つ
   ⑤keyseq でダイアログを開いたまま**他アプリ（メモ帳等）を前面にして入力中**の状態で Win+D を 2 回押す →
     **メモ帳が前面に戻り、keyseq がアクティブにならない**（タイトルバーの色で判定。v0.5: メモ帳のカーソルが出ない・文字が打てないのは
     keyseq 無しでも起きる Windows の挙動なので判定対象外・§1.2 測定 4）
   ⑥（§4-3 の確認）最小化せず Alt+Tab で離れて戻る → Escape が効くかを**記録**（効かなくても本フェーズでは直さない）
7. 正本 `features.md` §4.6 と `codebase_map.md` が §8 のとおり改訂される。

---

## §7 スコープ外（本フェーズでやらない）

- 最小化を伴わない再アクティブ化（Alt+Tab で最小化されていない窓へ戻る・クリック）でのフォーカス復帰（§4-3 の提案どおりなら）。
- 閉じた後のフォーカスの規定（§4-2 の提案どおりなら）。
- stdlib ダイアログ（`messagebox` 等）が開いている間の最小化（phase 17 から保証範囲外）。
- 最小化中にアプリ側が新しいモーダルを開いた場合の**復元できるか**（既存の保証範囲外 `features.md:218-219`・現状そのように開く経路は無い）。
  **復元できた場合の初期フォーカスは §3.1-6 で守る**（v0.3。v0.2 はこの経路全体をスコープ外としていたが、正本の初期フォーカス保証と衝突したため改めた）。
- idea_33（フック再開 flaky）/ 同型スケルトンの共通化。
- **（v0.5・受容する残存リスク）** ①§3.1-3 の前面判定と `focus_force` の間に OS の前面が他アプリへ変わる競合（TOCTOU）。
  ②**最小化中に開いた窓の初期フォーカス要求**（§3.1-6）: 1 回目の復元でアプリが OS フォーカスを持たないと要求が保留のまま記録だけ消え、
  2 回目の復元で窓自身へ `focus_set` されうる（`deep-reviewer` M1・未実測）。最小化中にモーダルを開く経路は現状無く、Escape は窓自身でも届くため
  **既知の限界として記録のみ**。経路が生まれた時点で対処する。

---

## §8 正本反映（フェーズ末昇格・予定）

- `instructions/common/spec_detail/features.md` §4.6「モーダルダイアログの作法」:
  - 最小化の条項「復元の際、アプリは窓の表示・前面を操作しない」を §4-1 の結果で改訂し、
    **フォーカス復帰の条項**（§3.1-1〜3）を追加。初期フォーカスの条項の「窓の前面化・フォーカス強制は行わない」も同じ言い回しに揃える
    （v0.5: 復元時だけ**条件つきで `focus_force` を使う**ため、初期フォーカスの条項〔`focus_force` を呼ばない〕とは言い回しを揃えつつ
    例外の範囲〔最小化からの復元・自アプリが前面・Tk がフォーカスを持たない〕を明記する）。
  - phase 28 の「閉じた後・最小化から復元した後のキーボードフォーカスの戻り先は規定しない」を §4-2 の結果で改訂。
  - （v0.5・`deep-reviewer` M2）**§3.1-6（最小化中に開いた窓には復帰しない・記録はその回の復元まで）も条項として昇格**し、
    「保証の範囲外」（`:218-219` 付近）の文言と突き合わせる。§7 の残存リスク①②を保証範囲外として書き添える。
    `after_idle`（§3.1-4）は実装の詳細なので `codebase_map.md` 側に書く。
- `instructions/common/codebase_map.md` の `modal.py` 節:
  `install_minimize_grab_custody` の説明にフォーカス復帰を追記 / `:312-313` の「推測型（`focus_lastfor()`）は不採用」を
  「**初期フォーカスでは**」と範囲を限定 / `:349` の「`deiconify` / `lift` / `focus_force` を呼ばない（中間窓が消える）」の理由を
  §1.1 のとおり正確にし、v0.5 の条件つき `focus_force` と `_is_app_foreground`（presentation 唯一の `ctypes` 利用）を追記する。
- 実装 = `keyseq/presentation/modal.py`。テスト = `tests_ui/test_minimize_grab_custody.py`（+ 必要なら実 Tk の到達検査）。
- idea_34 を `backlog/INDEX_done.md` へ移動。別実装同期: なし。

---

## 関連

- [idea_34](../backlog/idea_34_focus_restore_after_minimize.md) / 暫定仕様 [15](15_minimize_grab_custody.md)（最小化中の grab 預かり・凍結）/
  [22](22_dialog_keyboard_focus.md)（初期フォーカス・凍結）
- 正本: `features.md` §4.6「モーダルダイアログの作法」/ `codebase_map.md` の `modal.py` 節
- 判断履歴: `decisions_archive/17_minimize_grab_custody.md` / `decisions_archive/28_dialog_keyboard_focus.md`
