## 6. 非機能要件（安全設計）

* フック暴走を起こさないこと（最優先）
* suppress の副作用を考慮すること
* keyboard の例外は必ず吸収すること
* UI スレッドをブロックしないこと（UI 更新は `after()` を使用）
* JSON の後方互換を必ず維持すること
* キーボード操作の応答性を優先すること
* オニオンアーキテクチャの依存ルールを守ること（domain は UI・フック実装に非依存）

---
